---
phase: 03-floating-editor-context-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/grid/components/context-menu/contextMenu.svelte
  - frontend/src/lib/grid/components/context-menu/contextMenu.svelte.ts
autonomous: true
requirements: [F3.1, F4.1, F4.2, F4.3]

must_haves:
  truths:
    - "ContextMenu component accepts zero props — all state and actions come from gridContext"
    - "Right-clicking a cell shows the context menu at the correct screen position"
    - "Edit/Copy/Paste/FilterByValue actions invoke ctx.pageActions correctly"
    - "Delete row option appears only for newly-generated rows (row >= filteredAssetsCount)"
    - "ContextMenu hides itself when ctx.contextMenu.visible is false"
  artifacts:
    - path: "frontend/src/lib/grid/components/context-menu/contextMenu.svelte"
      provides: "Self-contained ContextMenu with zero props, reads ctx directly"
      contains: "getGridContext"
    - path: "frontend/src/lib/grid/components/context-menu/contextMenu.svelte.ts"
      provides: "ContextMenuState and internal handlers (handleFilterByValue, handleDeleteNewRow)"
      contains: "getGridContext"
  key_links:
    - from: "frontend/src/lib/grid/components/context-menu/contextMenu.svelte"
      to: "frontend/src/lib/context/gridContext.svelte.ts"
      via: "getGridContext() call"
      pattern: "getGridContext"
    - from: "frontend/src/lib/grid/components/context-menu/contextMenu.svelte"
      to: "ctx.pageActions"
      via: "onclick handlers calling ctx.pageActions?.onEditAction / onCopy / onPaste"
      pattern: "ctx\\.pageActions\\?"
---

<objective>
Refactor ContextMenu from a 7-prop component to a zero-prop, self-contained component that reads gridContext directly.

Purpose: ContextMenu is currently the only grid overlay still receiving action callbacks as props. Making it self-contained follows the established pattern (GridOverlays = 0 props, Toolbar = 1 prop) and eliminates the prop chain from +page.svelte.

Output: contextMenu.svelte with zero props; contextMenu.svelte.ts with all handlers inlined; +page.svelte simplifies to bare `<ContextMenu />`.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-floating-editor-context-menu/03-CONTEXT.md
@.planning/phases/03-floating-editor-context-menu/03-RESEARCH.md

# Key source files to read before implementing
@frontend/src/lib/grid/components/context-menu/contextMenu.svelte
@frontend/src/lib/grid/components/context-menu/contextMenu.svelte.ts
@frontend/src/lib/context/gridContext.svelte.ts
@frontend/src/routes/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor contextMenu.svelte.ts — inline handlers, add getGridContext</name>
  <files>frontend/src/lib/grid/components/context-menu/contextMenu.svelte.ts</files>
  <action>
Read the current contextMenu.svelte.ts to understand ContextMenuState and any existing logic.
Read +page.svelte to find the `handleFilterByValue` and `handleDeleteNewRow` functions (search for those names) — these currently live on the page and are passed as props.

Refactor contextMenu.svelte.ts to:
1. Import `getGridContext` from `$lib/context/gridContext.svelte.ts`
2. In the factory or as exported functions, add `handleFilterByValue()` and `handleDeleteNewRow()` — moving the logic verbatim from +page.svelte but replacing local `contextMenu` variable references with `ctx.contextMenu` and local `filteredAssets.length` with `ctx.filteredAssetsCount`.
3. The `handleFilterByValue` logic in +page.svelte searches for `ctx.contextMenu.key`/`ctx.contextMenu.value` and applies a filter via `searchManager` — replicate exactly, reading from ctx.
4. The `handleDeleteNewRow` logic deletes a newly-generated row using `ctx.assets` and `ctx.filteredAssetsCount` — replicate exactly.

Export these handlers from contextMenu.svelte.ts so contextMenu.svelte can import them, OR put them directly in the .svelte `<script>`.

Key constraint: `handleEditAction` in ContextMenu MUST pass explicit row/col — call `ctx.pageActions?.onEditAction('ctx', ctx.contextMenu!.row, ctx.contextMenu!.col)` — NOT rely on selection anchor (research finding 5).
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>contextMenu.svelte.ts has handleFilterByValue and handleDeleteNewRow; imports getGridContext; no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Refactor contextMenu.svelte to zero props + update +page.svelte</name>
  <files>
    frontend/src/lib/grid/components/context-menu/contextMenu.svelte
    frontend/src/routes/+page.svelte
  </files>
  <action>
Read contextMenu.svelte in full. Read the `<ContextMenu ...>` element in +page.svelte template to see the exact props being passed.

Refactor contextMenu.svelte:
1. Remove all `$props()` — the Props type with state, onEdit, onCopy, onPaste, onFilterByValue, onDelete, showDelete must be deleted entirely.
2. Add `import { getGridContext } from '$lib/context/gridContext.svelte.ts'` at top of script.
3. Add `const ctx = getGridContext()` inside script.
4. Compute `const showDelete = $derived((ctx.contextMenu?.row ?? -1) >= ctx.filteredAssetsCount)`.
5. All onclick handlers become direct calls to ctx.pageActions: `onCopy` → `ctx.pageActions?.onCopy()`, `onPaste` → `ctx.pageActions?.onPaste()`, etc.
6. Edit action: `ctx.pageActions?.onEditAction('ctx', ctx.contextMenu!.row, ctx.contextMenu!.col)` — explicit coords per research finding 5.
7. The root visibility guard: `{#if ctx.contextMenu?.visible}` — renders only when visible.
8. Position via inline style: `style="top:{ctx.contextMenu.y}px; left:{ctx.contextMenu.x}px;"` (fixed positioning).
9. Filter action calls `handleFilterByValue()` imported from contextMenu.svelte.ts (or defined inline).
10. Delete action calls `handleDeleteNewRow()` from contextMenu.svelte.ts.

Update +page.svelte:
1. Find the `<ContextMenu ...>` element and replace all props with bare `<ContextMenu />`.
2. Remove the `contextMenu` prop bindings — the component is self-contained.
3. Do NOT remove contextMenu state setup from +page.svelte (ctx.contextMenu is still set there).
4. Remove any now-unused imports from +page.svelte if handleFilterByValue/handleDeleteNewRow were defined there.

Run svelte-check and fix any type errors before finishing.
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -10</automated>
    <manual>Grep confirms zero props on ContextMenu in +page.svelte: grep -n "ContextMenu" frontend/src/routes/+page.svelte</manual>
  </verify>
  <done>svelte-check passes 0 errors; +page.svelte has `&lt;ContextMenu /&gt;` with no attribute props; contextMenu.svelte has getGridContext() and no $props()</done>
</task>

</tasks>

<verification>
1. `cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json` — must report 0 errors
2. `grep -n "ContextMenu" /home/joakim/asset_management/frontend/src/routes/+page.svelte` — must show `<ContextMenu />` with no attribute props
3. `grep -n "getGridContext\|ctx\.contextMenu\|ctx\.pageActions" /home/joakim/asset_management/frontend/src/lib/grid/components/context-menu/contextMenu.svelte` — must show context reads
4. `grep -n "\$props\|onEdit\|onCopy\|onPaste\|onFilterByValue\|onDelete" /home/joakim/asset_management/frontend/src/lib/grid/components/context-menu/contextMenu.svelte` — must return empty (no props)
</verification>

<success_criteria>
- ContextMenu.svelte has zero $props() — reads all state from getGridContext()
- +page.svelte renders `<ContextMenu />` with no attribute bindings
- All context menu actions (Edit, Copy, Paste, Filter, Delete) work via ctx.pageActions
- showDelete computed from ctx.filteredAssetsCount (not prop)
- svelte-check: 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-floating-editor-context-menu/03-01-SUMMARY.md`
</output>
