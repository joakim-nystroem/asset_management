---
phase: 03-floating-editor-context-menu
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - frontend/src/lib/components/grid/GridRow.svelte
  - frontend/src/lib/components/grid/GridContainer.svelte
  - frontend/src/lib/components/grid/GridOverlays.svelte
  - frontend/src/routes/+page.svelte
autonomous: true
requirements: [F3.1, F3.2, F3.3, F3.4, F3.5]

must_haves:
  truths:
    - "GridRow renders only cell display divs — no textarea, no EditDropdown, no Autocomplete"
    - "GridRow has 3 props: asset, keys, actualIndex — no editor or event props"
    - "Double-clicking a cell starts editing (ondblclick delegation in GridContainer data-layer)"
    - "Drag-selecting cells works (onmouseenter delegation in GridContainer data-layer)"
    - "FloatingEditor is mounted inside GridOverlays behind {#if ctx.isEditing}"
    - "svelte-check passes 0 errors after all changes"
  artifacts:
    - path: "frontend/src/lib/components/grid/GridRow.svelte"
      provides: "Pure display component rendering cell divs with data-row/data-col attributes"
      min_lines: 10
    - path: "frontend/src/lib/components/grid/GridContainer.svelte"
      provides: "Data-layer wrapper with ondblclick + onmouseenter + onmousedown delegation"
      contains: "ondblclick"
    - path: "frontend/src/lib/components/grid/GridOverlays.svelte"
      provides: "FloatingEditor mounted conditionally inside overlay layer"
      contains: "FloatingEditor"
  key_links:
    - from: "frontend/src/lib/components/grid/GridContainer.svelte"
      to: "ctx.pageActions.onEditAction"
      via: "ondblclick delegation reading dataset.row/dataset.col"
      pattern: "ondblclick.*onEditAction|onEditAction.*ondblclick"
    - from: "frontend/src/lib/components/grid/GridOverlays.svelte"
      to: "frontend/src/lib/grid/components/floating-editor/FloatingEditor.svelte"
      via: "{#if ctx.isEditing}<FloatingEditor />{/if}"
      pattern: "isEditing.*FloatingEditor|FloatingEditor.*isEditing"
---

<objective>
Wire FloatingEditor into GridOverlays, strip GridRow to a pure display component, and add event delegation to GridContainer. This is the integration plan that completes Phase 3.

Purpose: Plans 03-01 and 03-02 created the components in isolation. This plan connects them: mounts FloatingEditor in the overlay layer, promotes GridRow's event handlers to the GridContainer data-layer wrapper, and removes all editor markup from GridRow.

Output: GridRow ~25 lines (display only); GridContainer with proper event delegation; GridOverlays hosting FloatingEditor; +page.svelte with any remaining cleanup.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-floating-editor-context-menu/03-CONTEXT.md
@.planning/phases/03-floating-editor-context-menu/03-RESEARCH.md

# Prior plan summaries needed for wiring decisions
@.planning/phases/03-floating-editor-context-menu/03-01-SUMMARY.md
@.planning/phases/03-floating-editor-context-menu/03-02-SUMMARY.md

# Key source files — read all before modifying
@frontend/src/lib/components/grid/GridRow.svelte
@frontend/src/lib/components/grid/GridContainer.svelte
@frontend/src/lib/components/grid/GridOverlays.svelte
@frontend/src/routes/+page.svelte
@frontend/src/lib/context/gridContext.svelte.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip GridRow to pure display + add event delegation to GridContainer</name>
  <files>
    frontend/src/lib/components/grid/GridRow.svelte
    frontend/src/lib/components/grid/GridContainer.svelte
  </files>
  <action>
Read GridRow.svelte in full. Read GridContainer.svelte in full.

**Strip GridRow.svelte:**
Remove everything related to the inline editor:
- Props to remove: editDropdown, autocomplete, assets, onSaveEdit, onCancelEdit, onEditAction, onContextMenu, visibleIndex. Keep ONLY: asset, keys, actualIndex.
- Remove: all editor markup (the textarea block, EditDropdown, Autocomplete imports and elements)
- Remove: textareaRef $state and the $effect that focused it (this moves to FloatingEditor)
- Remove: per-cell event listeners: ondblclick, onmousedown, onmouseenter, oncontextmenu from cell divs
- Remove: the user prop (used only in the dblclick guard "Log in to edit" — this guard moves to GridContainer's delegation handler or is omitted since ctx.user is available from context)
- Keep: the {#each keys} loop with data-row={actualIndex} data-col={j} and the display span
- Keep: column width styling using createColumnController().getWidth(key)
- Remove: any imports that are no longer used (editDropdown.svelte, autocomplete.svelte, gridEdit, etc.)

The target is the pure display pattern from the research doc:
```svelte
<script lang="ts">
  import { getGridContext } from '$lib/context/gridContext.svelte.ts';
  import { createColumnController } from '$lib/grid/utils/gridColumns.svelte.ts';
  const ctx = getGridContext();
  const columns = createColumnController();
  type Props = { asset: Record<string, any>; keys: string[]; actualIndex: number; };
  let { asset, keys, actualIndex }: Props = $props();
</script>
{#each keys as key, j}
  <div data-row={actualIndex} data-col={j} class="..." style="width:{columns.getWidth(key)}px; min-width:{columns.getWidth(key)}px;">
    <span class="truncate w-full">{asset[key]}</span>
  </div>
{/each}
```

**Update GridContainer.svelte:**
Read the current data-layer wrapper div and its existing handlers. The wrapper already has onclick and oncontextmenu. Add the missing delegation handlers:

1. Read current GridRow rendering in GridContainer — find where GridRow is used and what props are passed. Update the GridRow element to pass only `asset={row.asset}`, `keys={keys}`, `actualIndex={row.actualIndex}` — remove all other props.

2. Add to the data-layer wrapper div:
   - `ondblclick={(e) => { const target = e.target as HTMLElement; const row = Number(target.closest('[data-row]')?.getAttribute('data-row') ?? target.dataset.row); const col = Number(target.closest('[data-col]')?.getAttribute('data-col') ?? target.dataset.col); if (!isNaN(row) && !isNaN(col)) ctx.pageActions?.onEditAction('dblclick', row, col); }}`
   - `onmouseenter={(e) => { ... }}` for drag selection — but check if this is currently on GridRow. If GridRow had `onmouseenter` per cell, the delegation needs to fire on the wrapper's mousemove or on the child div's mouseenter bubbling. The wrapper approach: read how `selection.extendSelection` is called in current GridRow and replicate the delegation pattern using `data-row`/`data-col` from `e.target`.
   - Verify `onmousedown` delegation is wired to `selection.handleMouseDown(row, col, e)` or equivalent — check the existing onclick/onmousedown in the wrapper and extend as needed.

3. Import createSelectionController if not already imported, use it for the mouseenter/mousedown handlers.

IMPORTANT — research pitfall 4: "After removing event listeners from GridRow cells, double-clicking a cell does nothing. Drag selection stops working." Verify ondblclick and onmouseenter are correctly delegated before finishing.

Run svelte-check after changes and fix any type errors.
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -10</automated>
    <manual>Verify GridRow is stripped: grep -c "textarea\|EditDropdown\|Autocomplete\|onSaveEdit\|onCancelEdit" frontend/src/lib/components/grid/GridRow.svelte (expect 0)</manual>
  </verify>
  <done>GridRow has 3 props only (asset/keys/actualIndex); no editor markup; GridContainer wrapper has ondblclick and onmouseenter delegation; svelte-check 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Mount FloatingEditor in GridOverlays + clean up +page.svelte</name>
  <files>
    frontend/src/lib/components/grid/GridOverlays.svelte
    frontend/src/routes/+page.svelte
  </files>
  <action>
Read GridOverlays.svelte in full. Read +page.svelte to find any remaining editor prop passing or ContextMenu props that plan 03-01 should have already cleaned up.

**Mount FloatingEditor in GridOverlays:**
1. Add import: `import FloatingEditor from '$lib/grid/components/floating-editor/FloatingEditor.svelte'`
2. Inside the GridOverlays template root div, after the existing overlay elements (selection, copy, dirty cell overlays), add:
   ```svelte
   {#if ctx.isEditing}
     <FloatingEditor />
   {/if}
   ```
   Per the architecture: FloatingEditor inside GridOverlays (which has `class="contents"`) means FloatingEditor is positioned relative to the virtual-chunk ancestor div — the same coordinate system as all other overlays. This is correct.

**Clean up +page.svelte:**
1. Find any GridRow prop passing that remains: if +page.svelte passes any editor props to GridRow via GridContainer, remove them. (GridContainer now handles the delegation directly.)
2. Verify the `<ContextMenu />` element has zero props (this was done in Plan 03-01, confirm it holds).
3. Verify the `handleEditAction` function in +page.svelte still works. Research finding 5 notes that `ctx.pageActions.onEditAction` currently ignores its row/col params and uses `getActionTarget()` internally. After Phase 3, ContextMenu calls `onEditAction('ctx', row, col)` with explicit coords and double-click delegation calls `onEditAction('dblclick', row, col)` with explicit coords. Update `handleEditAction` in +page.svelte to accept and use explicit row/col when provided:
   ```typescript
   async function handleEditAction(action?: string, row?: number, col?: number) {
     const target = (row !== undefined && col !== undefined)
       ? { row, col }
       : getActionTarget(); // fallback for keyboard F2 etc.
     // rest of handleEditAction unchanged, using target.row/target.col
   }
   ```
   Then update `ctx.pageActions.onEditAction = (action, row, col) => { handleEditAction(action, row, col); }`.
4. Remove any imports from +page.svelte that are now unused (e.g. if editDropdown/autocomplete were imported for GridRow props but now only needed for ctx channel setup — keep ctx setup, remove unused import-only references).

Run svelte-check and fix all errors before finishing.
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -10</automated>
    <manual>Confirm FloatingEditor is in GridOverlays: grep -n "FloatingEditor\|isEditing" frontend/src/lib/components/grid/GridOverlays.svelte</manual>
  </verify>
  <done>GridOverlays imports and conditionally mounts FloatingEditor; +page.svelte handleEditAction uses explicit row/col when provided; svelte-check 0 errors; full phase is functional</done>
</task>

</tasks>

<verification>
1. `cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json` — must report 0 errors
2. `grep -c "textarea\|onSaveEdit\|editDropdown\|autocomplete\|onCancelEdit" /home/joakim/asset_management/frontend/src/lib/components/grid/GridRow.svelte` — must be 0
3. `grep -n "Props\|\$props" /home/joakim/asset_management/frontend/src/lib/components/grid/GridRow.svelte` — must show only 3 props (asset, keys, actualIndex)
4. `grep -n "FloatingEditor\|isEditing" /home/joakim/asset_management/frontend/src/lib/components/grid/GridOverlays.svelte` — must show both
5. `grep -n "ondblclick" /home/joakim/asset_management/frontend/src/lib/components/grid/GridContainer.svelte` — must show delegation handler
6. `grep -n "ContextMenu" /home/joakim/asset_management/frontend/src/routes/+page.svelte` — must show `<ContextMenu />` with no attribute props
</verification>

<success_criteria>
- GridRow.svelte: 3 props (asset, keys, actualIndex), no editor markup, no event handlers per cell
- GridContainer.svelte: ondblclick and onmouseenter delegation on the data-layer wrapper
- GridOverlays.svelte: FloatingEditor mounted behind {#if ctx.isEditing}
- +page.svelte: handleEditAction uses explicit coords when provided; ContextMenu has zero props
- svelte-check: 0 errors after all changes
- Phase 3 goal met: GridRow is display-only; FloatingEditor positions autonomously; ContextMenu is self-contained
</success_criteria>

<output>
After completion, create `.planning/phases/03-floating-editor-context-menu/03-03-SUMMARY.md`
</output>
