---
phase: 01.2
slug: interaction-grid-layer
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-04
---

# Phase 01.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — no test framework installed; svelte-check is the automated gate |
| **Config file** | `frontend/tsconfig.json` (for svelte-check) |
| **Quick run command** | `cd frontend && npx svelte-check --tsconfig ./tsconfig.json` |
| **Full suite command** | `cd frontend && npx svelte-check --tsconfig ./tsconfig.json` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && npx svelte-check --tsconfig ./tsconfig.json`
- **After every plan wave:** Run `cd frontend && npx svelte-check --tsconfig ./tsconfig.json`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Behavior | Test Type | Automated Command | File Exists | Status |
|----------|-----------|-------------------|-------------|--------|
| TypeScript compiles 0 errors | type-check | `cd frontend && npx svelte-check --tsconfig ./tsconfig.json` | ✅ | ⬜ pending |
| Selection works (click, drag, shift-click) | manual | N/A | ❌ | ⬜ pending |
| Keyboard navigation (arrows, F2, Escape) | manual | N/A | ❌ | ⬜ pending |
| Context menu opens on right-click | manual | N/A | ❌ | ⬜ pending |
| Column resize works | manual | N/A | ❌ | ⬜ pending |
| Panel management (header menu, filter close on outside click) | manual | N/A | ❌ | ⬜ pending |
| Dirty cell overlays render correctly | manual | N/A | ❌ | ⬜ pending |
| GridOverlays has no event listeners after refactor | code review | N/A | ❌ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements — svelte-check is the automated verification gate. No test framework setup needed for this structural refactor phase.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Selection works (click, drag, shift-click) | N/A (inserted phase) | UI interaction — no test framework | Click cell → selected. Drag across cells → range selected. Shift-click → extends selection. |
| Keyboard navigation | N/A | UI interaction | Arrow keys move selection. F2 opens editor. Escape closes editor/clears selection. |
| Context menu | N/A | UI interaction | Right-click cell → context menu appears at click position with correct options. |
| Column resize | N/A | UI interaction | Drag column header border → column width updates in header and rows simultaneously. |
| Panel management | N/A | UI interaction | Open header menu → click outside → closes. Open filter panel → open header menu → filter closes. |
| Dirty cell overlays | N/A | Visual rendering | Edit cell → dirty overlay appears. Discard → overlay removed. |
| GridOverlays no event listeners | N/A | Structural | Code review: grep GridOverlays for `on:click`, `onclick`, `onmousedown`, `onkeydown` — should find none. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
