---
phase: 01.1-closepanels-helper-function
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/grid/components/grid-overlays/GridOverlays.svelte
autonomous: true
requirements: []

must_haves:
  truths:
    - "All panel-closing logic is consolidated in a single closePanels() function"
    - "closePanels() with no argument closes all 3 panels (contextMenu, headerMenu, filterPanel)"
    - "closePanels('contextMenu') closes headerMenu and filterPanel but NOT contextMenu"
    - "headerMenu.activeKey is reset to '' whenever headerMenu is closed"
    - "All 5 existing panel-closing sites use closePanels() instead of inline if-statements"
    - "Grid behavior is unchanged — panels open and close identically to before"
  artifacts:
    - path: "frontend/src/lib/grid/components/grid-overlays/GridOverlays.svelte"
      provides: "closePanels() helper function and all call sites"
      contains: "function closePanels"
  key_links:
    - from: "closePanels()"
      to: "uiCtx.contextMenu.visible, uiCtx.headerMenu.visible, uiCtx.headerMenu.activeKey, uiCtx.filterPanel.visible"
      via: "direct mutation of uiCtx properties"
      pattern: "closePanels\\("
---

<objective>
Extract the repeated panel-closing pattern into a reusable `closePanels()` helper function inside GridOverlays.svelte.

Purpose: Eliminate 5 instances of duplicated `if (uiCtx.X.visible) uiCtx.X.visible = false` pattern into one DRY helper, reducing code duplication and making future panel additions trivial.
Output: Modified GridOverlays.svelte with closePanels() function and all call sites updated.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-closepanels-helper-function/01.1-CONTEXT.md

@frontend/src/lib/grid/components/grid-overlays/GridOverlays.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create closePanels() helper and replace all inline panel-closing patterns</name>
  <files>frontend/src/lib/grid/components/grid-overlays/GridOverlays.svelte</files>
  <action>
Add a local `closePanels` function inside the `<script>` block of GridOverlays.svelte. Per user decision, it lives as a local function (not exported).

**Function signature** (use string literal union per Claude's discretion for type safety):

```typescript
function closePanels(except?: 'contextMenu' | 'headerMenu' | 'filterPanel') {
  if (except !== 'contextMenu' && uiCtx.contextMenu.visible) uiCtx.contextMenu.visible = false;
  if (except !== 'headerMenu' && uiCtx.headerMenu.visible) { uiCtx.headerMenu.activeKey = ''; uiCtx.headerMenu.visible = false; }
  if (except !== 'filterPanel' && uiCtx.filterPanel.visible) uiCtx.filterPanel.visible = false;
}
```

Place it after the context getter declarations (after line ~28, near the other helper functions), before the local UI state section.

**Replace ALL 5 call sites:**

1. **Escape key handler (~line 190-192):** Replace the 3 inline if-statements with `closePanels()` (no argument = close all).

2. **Resize handle in handleMouseDown (~line 298-300):** Replace the 3 inline if-statements with `closePanels()`.

3. **Cell click in handleMouseDown (~line 317-319):** Replace the 2 inline if-statements (headerMenu + filterPanel, NOT contextMenu) with `closePanels('contextMenu')` — this skips closing contextMenu per the `except` parameter.

4. **handleContextMenu (~line 369-370):** Replace the 2 inline if-statements (headerMenu + filterPanel) with `closePanels('contextMenu')`.

5. **onWindowClick (~line 397-399):** Replace the 3 inline if-statements (step 2 of the snapshot-close-reopen pattern) with `closePanels()`. The snapshot (step 1) and reopen (step 3) logic stays inline — only step 2 changes.

**Important:** Do NOT change any logic — only consolidate the existing pattern. The onWindowClick snapshot+reopen pattern must remain intact. The `except` parameter ensures contextMenu stays open when it should.
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - closePanels() function exists as a local function in GridOverlays.svelte
    - All 5 former inline panel-closing patterns replaced with closePanels() calls
    - Zero new svelte-check errors introduced
    - No behavioral change: panels open/close/toggle identically to before
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx svelte-check --tsconfig ./tsconfig.json` — zero new errors
2. Grep confirms no remaining inline panel-closing patterns outside closePanels():
   - `grep -n "uiCtx.contextMenu.visible = false" GridOverlays.svelte` should return only the line inside closePanels()
   - `grep -n "uiCtx.headerMenu.visible = false" GridOverlays.svelte` should return only the line inside closePanels()
   - `grep -n "uiCtx.filterPanel.visible = false" GridOverlays.svelte` should return only the line inside closePanels()
3. Grep confirms closePanels is called 5 times: `grep -c "closePanels(" GridOverlays.svelte` should be 6 (1 definition + 5 calls)
</verification>

<success_criteria>
- closePanels() helper exists with `except?` parameter using string literal union type
- All 5 inline panel-closing sites replaced with closePanels() or closePanels('contextMenu')
- headerMenu.activeKey reset is inside closePanels(), not at call sites
- svelte-check passes with no new errors
- Grid panel behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-closepanels-helper-function/01.1-01-SUMMARY.md`
</output>
