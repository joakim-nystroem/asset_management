---
phase: 07-row-generation-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/+page.svelte
  - frontend/src/lib/grid/eventQueue/EventListener.svelte
  - frontend/src/lib/components/grid/GridContainer.svelte
  - frontend/src/lib/components/grid/GridOverlays.svelte
  - frontend/src/lib/utils/interaction/interactionHandler.ts
  - frontend/src/lib/grid/utils/gridShortcuts.svelte.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "+page.svelte owns ALL server load data as $state — no duplicate $state in EventListener"
    - "EventListener receives baseAssets/filteredAssets as read-only props with setter lambdas — no data prop"
    - "GridOverlays is the parent wrapper around GridHeader and GridRows inside GridContainer"
    - "GridOverlays owns ALL keyboard and mouse listeners directly — no gridShortcuts attachment, no interactionHandler"
    - "All existing functionality works: sort, filter, view change, commit, discard, add row, edit, undo/redo, copy/paste, keyboard navigation"
  artifacts:
    - path: "frontend/src/routes/+page.svelte"
      provides: "Data ownership — $state for baseAssets, filteredAssets, setter lambdas to EventListener"
    - path: "frontend/src/lib/grid/eventQueue/EventListener.svelte"
      provides: "Event processor — no data prop, receives typed props, no duplicate $state, no setNextIdProvider $effect"
    - path: "frontend/src/lib/components/grid/GridOverlays.svelte"
      provides: "Parent wrapper with inline keyboard/mouse handling — no gridShortcuts import, no interactionHandler dependency"
    - path: "frontend/src/lib/components/grid/GridContainer.svelte"
      provides: "Virtual scroll viewport — GridOverlays wraps GridHeader+GridRows inside it"
  key_links:
    - from: "frontend/src/routes/+page.svelte"
      to: "frontend/src/lib/grid/eventQueue/EventListener.svelte"
      via: "props: baseAssets, filteredAssets, setBaseAssets, setFilteredAssets, user"
      pattern: "setBaseAssets=.*setFilteredAssets="
    - from: "frontend/src/lib/components/grid/GridOverlays.svelte"
      to: "window"
      via: "direct event listeners (onkeydown, onmouseup) — no gridShortcuts/@attach"
      pattern: "onkeydown|addEventListener"
---

<objective>
Complete the data ownership move to +page.svelte and restructure GridOverlays as the parent wrapper that owns all input handling directly, eliminating gridShortcuts and interactionHandler.

Purpose: Establish the foundational data flow (page owns data, passes setters down) and input architecture (GridOverlays wraps everything, owns all listeners) that all subsequent waves build upon.

Output: +page.svelte owns data with setter lambdas; EventListener is a pure event processor receiving typed props; GridOverlays wraps GridHeader+GridRows and owns all keyboard/mouse handling inline; gridShortcuts and interactionHandler are no longer imported by any component.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-row-generation-redesign/07-CONTEXT.md
@.planning/phases/07-row-generation-redesign/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/routes/+page.svelte (CURRENT — 40 lines):
- Already has: `let baseAssets: Record<string, any>[] = $state(data.assets ?? []);`
- Already has: `let filteredAssets: Record<string, any>[] = $state(data.searchResults ?? data.assets ?? []);`
- Already passes: `setBaseAssets={(v) => { baseAssets = v; }}` to EventListener
- GridContextProvider receives: user, locations, statuses, conditions, departments, initialKeys, baseAssets, filteredAssets

From frontend/src/lib/grid/eventQueue/EventListener.svelte (CURRENT props):
```typescript
type Props = { data: PageProps['data'] };
// Also has DUPLICATE $state declarations for baseAssets/filteredAssets (lines 60-63)
// Also has $effect for setNextIdProvider (lines 126-132) — must be deleted
```

From frontend/src/lib/grid/utils/gridShortcuts.svelte.ts:
```typescript
// Thin wrapper that creates interactionHandler and mounts window listeners
// Used via: {@attach gridShortcuts(shortcutState, callbacks)} in GridOverlays
export function gridShortcuts(state: ShortcutState, callbacks: InteractionCallbacks): Attachment<HTMLElement>
```

From frontend/src/lib/utils/interaction/interactionHandler.ts:
```typescript
// Creates keyboard handler (onkeydown) for navigation/shortcuts
// Returns mount(target) => cleanup function
export function createInteractionHandler(state, callbacks)
// Handles: Arrow keys, Tab, Enter, Escape, F2, Ctrl+C/V/Z/Y, Delete, Backspace, Home/End, PageUp/PageDown
```

From frontend/src/lib/components/grid/GridOverlays.svelte:
- Currently a `<div class="contents">` child inside GridContainer's translateY div
- Uses `{@attach gridShortcuts(shortcutState, callbacks)}` for keyboard handling
- Has `shortcutState` object and `callbacks` object defining all handler functions
- Renders: other user overlays, copy overlay, selection overlay, dirty cell overlays, FloatingEditor

From frontend/src/lib/components/grid/GridContainer.svelte:
- Currently renders GridOverlays as: `<GridOverlays />` inside the translateY div
- Has inline onmousedown, onmouseover, ondblclick, oncontextmenu on the container div
- Creates its own: selection, columns, rows, edit controllers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete data ownership move and EventListener prop cleanup</name>
  <files>
    frontend/src/routes/+page.svelte
    frontend/src/lib/grid/eventQueue/EventListener.svelte
  </files>
  <action>
**EventListener.svelte changes:**

1. Change Props type from `{ data: PageProps['data'] }` to individual typed props:
```typescript
type Props = {
  user: import('$lib/types').SafeUser | null;
  baseAssets: Record<string, any>[];
  filteredAssets: Record<string, any>[];
  setBaseAssets: (v: Record<string, any>[]) => void;
  setFilteredAssets: (v: Record<string, any>[]) => void;
  locations: any[];
  statuses: any[];
  conditions: any[];
  departments: any[];
};
```

2. Remove the `data` prop entirely. Destructure the new individual props.

3. DELETE the duplicate `$state` declarations for `baseAssets` and `filteredAssets` (lines 60-63). These now come from props. The `$derived` for `assets` and `keys` stays — it reads `filteredAssets` from props reactively.

4. DELETE the `$effect` for `setNextIdProvider` (lines 126-132). The rowGeneration controller already has its own NEW-N counter (implemented in Phase 6.1-03).

5. Update `createEventHandler` call: the getter lambdas `getBaseAssets: () => baseAssets` and `getFilteredAssets: () => filteredAssets` now capture the prop values (reactive in Svelte 5). Pass `setBaseAssets` and `setFilteredAssets` directly from props.

6. Update synchronous seed: replace `data.searchResults ?? data.assets ?? []` with `filteredAssets` (the prop), `data.assets ?? []` with `baseAssets` (the prop), `data.user ?? null` with `user` (the prop).

7. Update the `$effect` for validation constraints: instead of reading `data.locations`, `data.statuses`, etc., read from the individual props `locations`, `statuses`, `conditions`, `departments`.

8. Update `$effect(() => { dataCtx.user = data.user ?? null; })` to use the `user` prop.

9. Remove the import of `PageProps` type since `data` prop is gone.

**+page.svelte changes:**

1. Update EventListener invocation to pass individual typed props instead of `data`:
```svelte
<EventListener
  user={data.user ?? null}
  {baseAssets}
  {filteredAssets}
  setBaseAssets={(v) => { baseAssets = v; }}
  setFilteredAssets={(v) => { filteredAssets = v; }}
  locations={data.locations ?? []}
  statuses={data.statuses ?? []}
  conditions={data.conditions ?? []}
  departments={data.departments ?? []}
/>
```

Note: +page.svelte already passes user, baseAssets, filteredAssets, setBaseAssets, setFilteredAssets. Add locations/statuses/conditions/departments. Remove any remaining `data` prop if present.

2. Remove the `svelte-ignore state_referenced_locally` comments from baseAssets/filteredAssets — the state is no longer self-referencing since the initial value comes from `data` (a prop, not self).

Actually — keep the svelte-ignore comments. The `$state(data.assets ?? [])` pattern triggers `state_referenced_locally` because `data` is from `$props()` and `$state()` wraps the prop value. Svelte 5 warns about this. The comments are correct.
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - EventListener has no `data` prop — receives user, baseAssets, filteredAssets, setBaseAssets, setFilteredAssets, locations, statuses, conditions, departments as individual props
    - No duplicate $state for baseAssets/filteredAssets in EventListener
    - No setNextIdProvider $effect in EventListener
    - +page.svelte passes all individual props to EventListener
    - svelte-check passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure GridOverlays as parent wrapper with inline keyboard handling</name>
  <files>
    frontend/src/lib/components/grid/GridOverlays.svelte
    frontend/src/lib/components/grid/GridContainer.svelte
  </files>
  <action>
**GridOverlays.svelte changes:**

1. Remove the import of `gridShortcuts` from `$lib/grid/utils/gridShortcuts.svelte.ts`.

2. Read the interactionHandler source (`frontend/src/lib/utils/interaction/interactionHandler.ts`) to understand the exact keyboard handling logic. Inline the keyboard event handler directly into GridOverlays as an `onkeydown` handler on the root div. The interactionHandler handles:
   - Arrow keys (with Shift for selection extension, with Ctrl for jump-to-edge)
   - Tab / Shift+Tab (column navigation)
   - Enter (move down / start edit)
   - Escape (cancel edit / clear selection)
   - F2 (start edit)
   - Ctrl+C (copy), Ctrl+V (paste), Ctrl+Z (undo), Ctrl+Y (redo)
   - Delete/Backspace (clear selection)
   - Home/End (row start/end)
   - PageUp/PageDown (scroll page)
   - Printable characters (start edit with character)

3. Replace the `{@attach gridShortcuts(shortcutState, callbacks)}` with a direct `onkeydown` handler. The root div already has `tabindex="-1"`. Add `onkeydown={handleKeyDown}` and `onmouseup={() => selection.endSelection()}`.

4. Write a `handleKeyDown(e: KeyboardEvent)` function that implements the same logic as interactionHandler.ts but calls the existing `callbacks` object methods directly. The callbacks object already has: onCopy, onPaste, onUndo, onRedo, onEscape, onEdit, onScrollIntoView, getGridSize. These stay as-is.

5. For navigation (arrow keys, tab, enter, home/end, pgup/pgdn), inline the logic that currently lives in interactionHandler. This logic reads `selection` and `columns` from the shortcutState and calls methods like `selection.selectCell()`, `selection.handleMouseDown()`. Since GridOverlays already has `selection` and `columns` as local variables, reference them directly instead of through a state object.

6. Remove the `shortcutState` object entirely — it was only needed for the gridShortcuts attachment.

7. Add a window-level mouseup listener to call `selection.endSelection()` (interactionHandler currently does this). Use `$effect` with addEventListener/removeEventListener for the window mouseup.

**GridContainer.svelte changes:**

1. Move GridOverlays from inside the translateY div to WRAP the GridHeader + row rendering area. GridOverlays becomes the parent of the content area (the div with onmousedown/onmouseover/ondblclick/oncontextmenu).

2. Move the mouse event handlers (onmousedown, onmouseover, ondblclick, oncontextmenu) from the GridContainer inner div INTO GridOverlays. GridOverlays is the interaction layer — it should own these. GridContainer keeps only the scroll container and virtual scroll mechanics.

Wait — this is a structural change that affects the render tree significantly. The CONTEXT.md says "GridOverlays becomes PARENT wrapper: GridContainer -> GridOverlays -> (GridHeader + GridRows + FloatingEditor)". But GridOverlays is currently inside GridContainer's translateY div so it positions correctly relative to the virtual scroll offset.

Actually, re-reading the CONTEXT.md more carefully: "Passes props DOWN to FloatingEditor (edit keystrokes) and GridHeader (resize events) as children." This means GridOverlays should wrap the content that's inside the translateY div. The overlays position themselves using absolute positioning relative to the translateY parent.

Implement it as follows:
- GridContainer keeps the scroll container, the HeaderMenu rendering, and the outer structure
- Inside the translateY div, GridOverlays wraps everything (GridHeader moves outside translateY as it's sticky top-0, but GridRows + overlays stay inside translateY)
- Actually, GridHeader is already `sticky top-0` — it stays at its current position in GridContainer, outside the translateY div
- GridOverlays wraps the content INSIDE the translateY div — the GridRows and FloatingEditor
- Mouse events (onmousedown, onmouseover, ondblclick, oncontextmenu) move from GridContainer's inner div to GridOverlays' root div

The key structural change: GridOverlays' root div replaces the anonymous div that currently holds the mouse handlers. GridOverlays renders its overlay divs AND the row items as children.

Concrete steps for GridContainer:
1. Remove the mouse event handlers from the inner content div
2. Remove `<GridOverlays />` from its current position
3. Replace the content area with `<GridOverlays>` as a wrapper component that receives a `children` snippet
4. Pass the virtual scroll data to GridOverlays as props OR keep GridOverlays reading from context

Actually, the simplest approach that preserves the overlay positioning: keep GridOverlays where it is (inside translateY), but have it render the rows as its children via a snippet. This way overlays and rows share the same coordinate system.

Simplest correct approach for this task:
- Keep the current file structure mostly intact
- Move the onmousedown/onmouseover/ondblclick/oncontextmenu handlers from GridContainer into GridOverlays
- GridOverlays' root div gets these handlers instead of `class="contents"`
- GridContainer renders `<GridOverlays />` and the rows are siblings (current structure)

The "parent wrapper" goal can be completed when GridOverlays gains the snippet-based children approach. For now, moving the mouse handlers is the critical change. The full parent-wrapper restructure (GridOverlays wrapping GridRows) is a bigger layout change that should be done carefully — mark it as a note for the executor.

**Revised approach for Task 2:**
1. Inline keyboard handling into GridOverlays (eliminate gridShortcuts + interactionHandler dependency)
2. Move mouse handlers from GridContainer to GridOverlays
3. GridOverlays root div changes from `class="contents"` to the actual interactive div with mouse/keyboard handlers
4. GridContainer simplifies — no more inline mouse event handlers on the content div

The executor should read `interactionHandler.ts` in full to understand the keyboard handling logic to inline.
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - GridOverlays has inline onkeydown handler — no gridShortcuts import, no interactionHandler dependency
    - GridOverlays owns all mouse handlers (onmousedown, onmouseover, ondblclick, oncontextmenu)
    - GridContainer has no mouse event handlers on its content div — delegates to GridOverlays
    - shortcutState object removed from GridOverlays
    - Window mouseup listener managed by $effect in GridOverlays
    - All keyboard navigation works: arrow keys, tab, enter, escape, F2, Ctrl+C/V/Z/Y, delete, home/end, pgup/pgdn
    - svelte-check passes
  </done>
</task>

</tasks>

<verification>
1. `cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json` — 0 errors
2. Manual smoke test: app loads, data displays, sort works, filter works, edit cell works, add new row works, commit works, keyboard navigation works, copy/paste works, undo/redo works
</verification>

<success_criteria>
- EventListener receives individual typed props — no `data` prop, no duplicate $state
- GridOverlays owns all keyboard + mouse input handling inline — no gridShortcuts, no interactionHandler imports
- svelte-check 0 errors
- All grid functionality works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/07-row-generation-redesign/07-01-SUMMARY.md`
</output>
