---
phase: 07-row-generation-redesign
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - frontend/src/lib/grid/utils/gridValidation.svelte.ts
  - frontend/src/lib/grid/utils/gridChanges.svelte.ts
  - frontend/src/lib/grid/utils/gridEdit.svelte.ts
  - frontend/src/lib/grid/utils/gridHistory.svelte.ts
  - frontend/src/lib/grid/utils/gridSelection.svelte.ts
  - frontend/src/lib/grid/utils/gridColumns.svelte.ts
  - frontend/src/lib/grid/utils/gridRows.svelte.ts
  - frontend/src/lib/grid/utils/gridClipboard.svelte.ts
  - frontend/src/lib/grid/utils/gridShortcuts.svelte.ts
  - frontend/src/lib/grid/utils/rowGeneration.svelte.ts
  - frontend/src/lib/utils/interaction/interactionHandler.ts
  - frontend/src/lib/context/gridContext.svelte.ts
  - frontend/src/lib/context/GridContextProvider.svelte
  - frontend/src/lib/grid/eventQueue/EventListener.svelte
  - frontend/src/lib/grid/eventQueue/EventHandler.svelte.ts
  - frontend/src/lib/components/grid/GridContainer.svelte
  - frontend/src/lib/components/grid/GridOverlays.svelte
autonomous: true
requirements: []

must_haves:
  truths:
    - "ALL 11 legacy controller files are deleted from the repository"
    - "interactionHandler.ts is deleted"
    - "No file in frontend/src/ imports any of the deleted controller files"
    - "GridContextProvider creates no controller instances — only initializes context shells (editCtx, historyCtx, newRowCtx, and the existing domain contexts)"
    - "gridContext.svelte.ts has no type imports from deleted controller files — no ChangeController, HistoryController, RowGenerationController, SelectionController types"
    - "gridContext.svelte.ts has no context pairs for deleted controllers — no getChangeControllerContext, getHistoryControllerContext, getRowGenControllerContext"
    - "EventHandler.svelte.ts uses editCtx, historyCtx, newRow utility — no controller type imports"
    - "EventListener.svelte uses newRow utility, editCtx, historyCtx, selectionCtx directly — no createSelectionController, no controller context getters"
    - "svelte-check passes with 0 errors"
    - "All grid functionality works: sort, filter, view change, commit, discard, add row, edit, undo/redo, copy/paste, keyboard navigation"
  artifacts:
    - path: "frontend/src/lib/context/gridContext.svelte.ts"
      provides: "Clean context types — no imports from deleted controller files, no ChangeController/HistoryController/RowGenerationController/SelectionController context pairs"
    - path: "frontend/src/lib/context/GridContextProvider.svelte"
      provides: "Pure context shell factory — no createChangeController/createHistoryController/createRowGenerationController, only $state context initialization"
  key_links:
    - from: "frontend/src/lib/context/gridContext.svelte.ts"
      to: "deleted files"
      via: "no import references"
      pattern: "import.*gridChanges|import.*gridHistory|import.*rowGeneration|import.*gridSelection"
---

<objective>
Delete all 11 legacy controller files and interactionHandler.ts. Clean up all remaining references — remove stale context pairs, type imports, and controller creation. Replace any remaining controller usage with direct context reads or newRow utility. Final svelte-check gate.

Purpose: Complete the architectural correction. After this plan, every file in the codebase matches the target architecture defined in CLAUDE.md — components own their logic, contexts hold ephemeral state, no external controllers remain.

Output: 11 controller files + interactionHandler.ts deleted. Zero imports of deleted files. gridContext.svelte.ts clean (no controller types or context pairs). GridContextProvider is pure shell factory. svelte-check 0 errors.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-row-generation-redesign/07-CONTEXT.md
@.planning/phases/07-row-generation-redesign/07-01-SUMMARY.md
@.planning/phases/07-row-generation-redesign/07-02-SUMMARY.md
@.planning/phases/07-row-generation-redesign/07-03-SUMMARY.md

<interfaces>
<!-- Files to delete (11 controllers + 1 handler): -->
1.  frontend/src/lib/grid/utils/gridValidation.svelte.ts
2.  frontend/src/lib/grid/utils/gridChanges.svelte.ts
3.  frontend/src/lib/grid/utils/gridEdit.svelte.ts
4.  frontend/src/lib/grid/utils/gridHistory.svelte.ts
5.  frontend/src/lib/grid/utils/gridSelection.svelte.ts
6.  frontend/src/lib/grid/utils/gridColumns.svelte.ts
7.  frontend/src/lib/grid/utils/gridRows.svelte.ts
8.  frontend/src/lib/grid/utils/gridClipboard.svelte.ts
9.  frontend/src/lib/grid/utils/gridShortcuts.svelte.ts
10. frontend/src/lib/grid/utils/rowGeneration.svelte.ts
11. frontend/src/lib/utils/interaction/interactionHandler.ts

<!-- Concrete replacements (from Plans 01-03): -->
- gridShortcuts + interactionHandler → GridOverlays inline keyboard handler (Plan 01)
- gridClipboard copy → GridOverlays inline copy (Plan 01), paste → FloatingEditor (Plan 02)
- gridEdit → FloatingEditor.svelte.ts startEdit/save/cancel (Plan 02)
- gridChanges → editCtx.edits (Plan 02)
- gridHistory → historyCtx + FloatingEditor undo/redo (Plan 02)
- gridValidation → FloatingEditor.isValidValue (Plan 02)
- rowGeneration → newRow.svelte.ts createNewRowUtils (Plan 02)
- gridColumns → GridHeader inline + colCtx reads (Plan 03)
- gridRows → rowCtx reads (Plan 03)
- gridSelection → GridOverlays + selectionCtx direct writes (Plan 01)

<!-- Files that currently import from deleted files (must be cleaned in Task 1): -->

gridContext.svelte.ts imports:
- type { ChangeController } from gridChanges
- type { HistoryController } from gridHistory
- type { RowGenerationController } from rowGeneration
- Exports 3 controller context pairs: getChangeControllerContext/setChangeControllerContext,
  getHistoryControllerContext/setHistoryControllerContext, getRowGenControllerContext/setRowGenControllerContext

GridContextProvider.svelte imports:
- createChangeController from gridChanges
- createHistoryController from gridHistory
- createRowGenerationController from rowGeneration

EventHandler.svelte.ts imports (should already be updated by Plan 02, verify):
- type { ChangeController } from gridChanges
- type { HistoryController } from gridHistory
- type { SelectionController } from gridSelection
- type { RowGenerationController } from rowGeneration

EventListener.svelte imports (should already be updated by Plan 02, verify):
- createSelectionController from gridSelection
- createValidationController from gridValidation
- getChangeControllerContext, getHistoryControllerContext, getRowGenControllerContext

GridOverlays.svelte (should already be updated by Plans 01-02, verify):
- createClipboardController from gridClipboard (paste may still use this — move paste inline or to FloatingEditor)
- createSelectionController from gridSelection
- createEditController from gridEdit

GridContainer.svelte (should already be updated by Plans 01+03, verify):
- createColumnController from gridColumns
- createRowController from gridRows
- createSelectionController from gridSelection
- createEditController from gridEdit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix all remaining imports of controller files</name>
  <files>
    frontend/src/lib/context/gridContext.svelte.ts
    frontend/src/lib/context/GridContextProvider.svelte
    frontend/src/lib/grid/eventQueue/EventHandler.svelte.ts
    frontend/src/lib/grid/eventQueue/EventListener.svelte
    frontend/src/lib/components/grid/GridOverlays.svelte
    frontend/src/lib/components/grid/GridContainer.svelte
  </files>
  <action>
Run a comprehensive search for ALL imports of the 11 controller files + interactionHandler across the entire frontend/src/ directory:

```bash
grep -rn "gridValidation\|gridChanges\|gridEdit\|gridHistory\|gridSelection\|gridColumns\|gridRows\|gridClipboard\|gridShortcuts\|rowGeneration\|interactionHandler" frontend/src/ --include="*.ts" --include="*.svelte"
```

For EVERY match found, fix it. The concrete replacements are specified below — no options, no decisions left:

**gridContext.svelte.ts:**
1. Remove `import type { ChangeController }` from gridChanges.
2. Remove `import type { HistoryController }` from gridHistory.
3. Remove `import type { RowGenerationController }` from rowGeneration.
4. Remove the 3 controller context pairs:
   - `export const [getChangeControllerContext, setChangeControllerContext] = createContext<ChangeController>();`
   - `export const [getHistoryControllerContext, setHistoryControllerContext] = createContext<HistoryController>();`
   - `export const [getRowGenControllerContext, setRowGenControllerContext] = createContext<RowGenerationController>();`
5. Keep the NEW contexts added in Plan 02: CellEdit, EditContext, HistoryContext, HistoryAction, NewRowContext and their context pairs.
6. Keep ChangeContext type if other code references it. If nothing references it after Plan 02 cleanup, remove it.

**GridContextProvider.svelte:**
1. Remove `import { createChangeController }` from gridChanges.
2. Remove `import { createHistoryController }` from gridHistory.
3. Remove `import { createRowGenerationController }` from rowGeneration.
4. Remove the controller creation and context publication:
   - Delete `const changeController = createChangeController();` and `setChangeControllerContext(changeController);`
   - Delete `const historyController = createHistoryController();` and `setHistoryControllerContext(historyController);`
   - Delete `const rowGenController = createRowGenerationController();` and `setRowGenControllerContext(rowGenController);`
5. Remove `import { setChangeControllerContext, setHistoryControllerContext, setRowGenControllerContext }` from gridContext.svelte.ts.
6. GridContextProvider should now be a PURE shell factory: only `$state({...})` objects and `set*Context()` calls for domain contexts (editingCtx, selectionCtx, clipboardCtx, columnCtx, rowCtx, sortCtx, validationCtx, changeCtx, dataCtx, viewCtx, uiCtx) plus the NEW contexts (editCtx, historyCtx, newRowCtx). No controller factories. No business logic.

**EventHandler.svelte.ts:**
1. Remove ALL type imports from gridChanges, gridHistory, gridSelection, rowGeneration.
2. Verify that Plan 02 already replaced the `EventHandlerDeps` type to use editCtx/historyCtx/newRow utility. If any stale references remain, fix:
   - `changes: ChangeController` → already replaced with editCtx-based reads (Plan 02)
   - `history: HistoryController` → already replaced with historyCtx-based reads (Plan 02)
   - `selection: SelectionController` → replace with direct writes to selectionCtx: `selectionCtx.selectionStart = {row: -1, col: -1}` etc. for reset/clear operations. The `selection.selectCell()` calls become direct selectionCtx writes.
   - `rowGen: RowGenerationController` → already replaced with `newRow: NewRowUtils` (Plan 02)

**EventListener.svelte:**
1. Remove `import { createSelectionController }`. Replace ALL `selection.xyz()` calls with direct selectionCtx writes:
   - `selection.reset()` → `selectionCtx.selectionStart = {row: -1, col: -1}; selectionCtx.selectionEnd = {row: -1, col: -1};`
   - `selection.selectCell(row, col)` → `selectionCtx.selectionStart = {row, col}; selectionCtx.selectionEnd = {row, col};`
   - `selection.clearDirtyCells()` → `selectionCtx.dirtyCells = new Set();`
   - `selection.resetAll()` → reset start/end + clear dirtyCells + isSelecting=false + isHiddenAfterCopy=false
   - `selection.setDirtyCells(cells)` → only needed if dirty cell tracking $effect still exists (it should have been removed in Plan 02). If it's gone, remove this too.
2. Remove `import { createValidationController }` if still present. The `validation.setConstraints()` call should have been removed in Plan 02. If not, remove it — the $effect that writes to `validationCtx.validationConstraints` is sufficient.
3. Remove ALL `getChangeControllerContext`, `getHistoryControllerContext`, `getRowGenControllerContext` imports. These context pairs are being deleted from gridContext.svelte.ts.
4. Verify Plan 02 already replaced these with editCtx/historyCtx/newRow utility references.

**GridOverlays.svelte:**
1. If `createClipboardController` import still exists (for paste), replace with inline paste logic or import paste as a standalone utility. Per CLAUDE.md: "Copy is handled by GridOverlays (read-only). Paste is handled by FloatingEditor (edit operation)." If Plan 02 moved paste to FloatingEditor, the clipboard controller import should already be gone. If not, move paste to FloatingEditor now:
   - The paste logic from gridClipboard reads from system clipboard, applies values to assets, returns changes
   - Move it to floatingEditor.svelte.ts as a `paste()` method that reads clipboard, applies values, and runs upsertEdit for each change
   - GridOverlays' `onPaste` callback calls FloatingEditor's paste method
2. Remove `createSelectionController` — replaced by selectionCtx direct reads/writes (Plan 01 should have done this).
3. Remove `createEditController` — replaced by FloatingEditor's expanded controller (Plan 02).
4. Verify no other imports of deleted files remain.

**GridContainer.svelte:**
1. Verify no imports of deleted controllers remain after Plan 03. Fix any stragglers.
2. Specifically verify: no `createColumnController`, `createRowController`, `createSelectionController`, `createEditController`.

**IMPORTANT:** For each file, after removing the import, also remove any code that USES the imported symbol. Do not leave dead references.

After all fixes, re-run the grep to confirm zero matches.
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && grep -rn "gridValidation\|gridChanges\|gridEdit\|gridHistory\|gridSelection\|gridColumns\|gridRows\|gridClipboard\|gridShortcuts\|rowGeneration\|interactionHandler" src/ --include="*.ts" --include="*.svelte" | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>
    - Zero imports of any of the 11 controller files or interactionHandler across all of frontend/src/
    - gridContext.svelte.ts has no type imports from deleted files, no controller context pairs
    - GridContextProvider creates no controller instances — pure context shell factory with domain contexts + editCtx/historyCtx/newRowCtx
    - EventHandler uses editCtx/historyCtx/newRow utility — no controller type imports
    - EventListener uses selectionCtx directly — no createSelectionController
    - EventListener uses newRow utility — no getRowGenControllerContext
    - GridOverlays has no clipboard/selection/edit controller imports
    - svelte-check passes (run as part of Task 2)
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete all 11 controller files + interactionHandler.ts, final svelte-check gate</name>
  <files>
    frontend/src/lib/grid/utils/gridValidation.svelte.ts
    frontend/src/lib/grid/utils/gridChanges.svelte.ts
    frontend/src/lib/grid/utils/gridEdit.svelte.ts
    frontend/src/lib/grid/utils/gridHistory.svelte.ts
    frontend/src/lib/grid/utils/gridSelection.svelte.ts
    frontend/src/lib/grid/utils/gridColumns.svelte.ts
    frontend/src/lib/grid/utils/gridRows.svelte.ts
    frontend/src/lib/grid/utils/gridClipboard.svelte.ts
    frontend/src/lib/grid/utils/gridShortcuts.svelte.ts
    frontend/src/lib/grid/utils/rowGeneration.svelte.ts
    frontend/src/lib/utils/interaction/interactionHandler.ts
  </files>
  <action>
1. Delete all 11 controller files:
```bash
rm frontend/src/lib/grid/utils/gridValidation.svelte.ts
rm frontend/src/lib/grid/utils/gridChanges.svelte.ts
rm frontend/src/lib/grid/utils/gridEdit.svelte.ts
rm frontend/src/lib/grid/utils/gridHistory.svelte.ts
rm frontend/src/lib/grid/utils/gridSelection.svelte.ts
rm frontend/src/lib/grid/utils/gridColumns.svelte.ts
rm frontend/src/lib/grid/utils/gridRows.svelte.ts
rm frontend/src/lib/grid/utils/gridClipboard.svelte.ts
rm frontend/src/lib/grid/utils/gridShortcuts.svelte.ts
rm frontend/src/lib/grid/utils/rowGeneration.svelte.ts
rm frontend/src/lib/utils/interaction/interactionHandler.ts
```

2. Run `svelte-check`:
```bash
cd frontend && npx svelte-check --tsconfig ./tsconfig.json
```

3. If svelte-check reports errors about missing files, fix the remaining imports in the reported files (Task 1 should have caught them all, but this is the safety net).

4. After svelte-check passes with 0 errors, verify the remaining files in `frontend/src/lib/grid/utils/`:
```bash
ls frontend/src/lib/grid/utils/
```
Expected remaining: `virtualScrollManager.svelte.ts` only.

5. Verify `frontend/src/lib/utils/interaction/`:
```bash
ls frontend/src/lib/utils/interaction/
```
Expected remaining: `realtimeManager.svelte.ts` only.

6. Run a final comprehensive audit:
```bash
# Confirm no dead imports
grep -rn "from.*grid/utils/grid" frontend/src/ --include="*.ts" --include="*.svelte"
# Should only match virtualScrollManager references
```
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 11 controller files deleted: gridValidation, gridChanges, gridEdit, gridHistory, gridSelection, gridColumns, gridRows, gridClipboard, gridShortcuts, rowGeneration
    - interactionHandler.ts deleted
    - frontend/src/lib/grid/utils/ contains only virtualScrollManager.svelte.ts
    - frontend/src/lib/utils/interaction/ contains only realtimeManager.svelte.ts
    - svelte-check 0 errors
    - All grid functionality works end-to-end
  </done>
</task>

</tasks>

<verification>
1. `cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json` — 0 errors
2. `ls frontend/src/lib/grid/utils/` — only virtualScrollManager.svelte.ts
3. `ls frontend/src/lib/utils/interaction/` — only realtimeManager.svelte.ts
4. `grep -rn "gridValidation\|gridChanges\|gridEdit\|gridHistory\|gridSelection\|gridColumns\|gridRows\|gridClipboard\|gridShortcuts\|rowGeneration\|interactionHandler" frontend/src/ --include="*.ts" --include="*.svelte"` — zero matches
5. Functional: all grid operations work — sort, filter, view change, commit, discard, add row, edit, undo/redo, copy/paste, keyboard navigation, column resize
</verification>

<success_criteria>
- ALL 11 legacy controller files deleted from repository
- interactionHandler.ts deleted
- Zero remaining imports of deleted files
- GridContextProvider is a pure context shell factory — no controller creation
- gridContext.svelte.ts has no type imports from deleted files, no controller context pairs
- EventHandler uses newRow utility, editCtx, historyCtx — verified
- EventListener uses selectionCtx directly, newRow utility — no controller wrappers
- svelte-check 0 errors
- All grid functionality works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/07-row-generation-redesign/07-04-SUMMARY.md`
</output>
</content>
</invoke>