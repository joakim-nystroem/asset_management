---
phase: 07-row-generation-redesign
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - frontend/src/lib/grid/eventQueue/EventListener.svelte
  - frontend/src/lib/context/gridContext.svelte.ts
  - frontend/src/lib/context/GridContextProvider.svelte
autonomous: true
requirements: []
must_haves:
  truths:
    - "handleFilterSelect is removed from EventListener — filter select goes URL → $effect → queue"
    - "uiCtx no longer has handleFilterSelect or applySort fields"
    - "EventListener is a pure event processor: queue wiring, $effects, URL sync, action callbacks"
    - "All existing functionality works: sort, filter, view change, commit, discard, add row, edit, undo/redo"
    - "svelte-check passes with 0 errors"
  artifacts:
    - path: "frontend/src/lib/grid/eventQueue/EventListener.svelte"
      provides: "Pure event processor — no sort, simplified filter select"
    - path: "frontend/src/lib/context/gridContext.svelte.ts"
      provides: "Clean UiContext type without applySort/handleFilterSelect"
  key_links:
    - from: "frontend/src/lib/grid/components/header-menu/headerMenu.svelte"
      to: "frontend/src/lib/data/searchManager.svelte"
      via: "onFilterSelect → searchManager"
      pattern: "onFilterSelect"
    - from: "frontend/src/lib/grid/eventQueue/EventListener.svelte"
      to: "frontend/src/lib/grid/eventQueue/EventQueue.svelte.ts"
      via: "URL $effect → queue.enqueue"
      pattern: "queue\\.enqueue"
---

<objective>
Remove filter selection indirection from EventListener and clean up UiContext callback fields. Final svelte-check gate.

Purpose: Currently `handleFilterSelect` in EventListener is a thin wrapper that reads URL state, toggles a filter, and calls `updateSearchUrl` — which triggers the URL `$effect` → queue enqueue. The HeaderMenu and ContextMenu already call this through `uiCtx.handleFilterSelect`. After this plan, the filter selection flow is simplified: HeaderMenu/ContextMenu call `uiCtx.handleFilterSelect` which is now a simpler function that just updates the URL. Also clean up the `applySort` and `handleFilterSelect` fields from UiContext since `applySort` is now owned by GridHeader (registered via $effect in Plan 02).

Output: EventListener is a pure event processor. UiContext is clean. All functionality verified.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-row-generation-redesign/07-CONTEXT.md
@.planning/phases/07-row-generation-redesign/07-01-SUMMARY.md
@.planning/phases/07-row-generation-redesign/07-02-SUMMARY.md

<interfaces>
<!-- After Plan 02, UiContext type includes: -->
From gridContext.svelte.ts:
```typescript
export type UiContext = {
  filterPanel: FilterPanelState | null;
  headerMenu: any | null;
  contextMenu: any | null;
  handleFilterSelect: ((item: string, key: string) => void) | null;
  applySort: ((key: string, dir: 'asc' | 'desc') => void) | null;
  getCurrentUrlState?: () => { q: string; filters: Filter[]; view: string };
  updateSearchUrl?: (params: { q?: string; filters?: Filter[]; view?: string }) => void;
};
```

From headerMenu.svelte — how filter select is called:
```svelte
onFilterSelect={(item, key) => uiCtx.handleFilterSelect?.(item, key)}
```
<!-- GridContainer passes this callback to HeaderMenu from uiCtx -->

From contextMenu.svelte.ts — how filter select is called:
```typescript
uiCtx.handleFilterSelect?.(filterValue, key);
```

From EventListener.svelte handleFilterSelect (current):
```typescript
function handleFilterSelect(item: string, key: string) {
  const { q, filters, view } = getCurrentUrlState();
  const exists = filters.some(f => f.key === key && f.value === item);
  const newFilters = exists
    ? filters.filter(f => !(f.key === key && f.value === item))
    : [...filters, { key, value: item }];
  updateSearchUrl({ q, filters: newFilters, view });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up filter select indirection and UiContext fields</name>
  <files>
    frontend/src/lib/grid/eventQueue/EventListener.svelte
    frontend/src/lib/context/gridContext.svelte.ts
    frontend/src/lib/context/GridContextProvider.svelte
  </files>
  <action>
**Analysis:** `handleFilterSelect` in EventListener uses `getCurrentUrlState()` and `updateSearchUrl()` — both are local functions in EventListener. These functions use the `reactiveUrl` SvelteURL which is also local to EventListener. So `handleFilterSelect` MUST stay in EventListener (it depends on EventListener's URL state). The function itself is already clean — it's just URL manipulation that triggers the URL $effect → queue enqueue flow.

The cleanup here is:
1. Keep `handleFilterSelect` in EventListener (it needs the URL helpers).
2. Keep the $effect that registers `uiCtx.handleFilterSelect = handleFilterSelect`.
3. Remove `uiCtx.applySort = applySort` from EventListener's $effect (already done in Plan 02 — verify it's gone).
4. Clean up UiContext type: `applySort` should stay as a field (GridHeader registers it via $effect in Plan 02). `handleFilterSelect` should stay as a field (EventListener registers it). These ARE the intended patterns — the callback fields on UiContext are the communication channel.

**Revised scope:** The filter extraction per CONTEXT.md says "Components write searchManager directly, EventListener reacts." Let's verify this is already the case:
- HeaderMenu.svelte writes filter selections via `onFilterSelect` callback → GridContainer → `uiCtx.handleFilterSelect` → EventListener's `handleFilterSelect` → `updateSearchUrl` → URL $effect → queue enqueue FILTER
- ContextMenu.svelte.ts calls `uiCtx.handleFilterSelect?.(filterValue, key)`

The indirection is: component → uiCtx callback → EventListener function → URL update → $effect → queue. The CONTEXT.md target says "components write searchManager directly." But `handleFilterSelect` doesn't write searchManager — it updates the URL, which triggers the URL $effect which syncs searchManager and enqueues. The searchManager is synced FROM the URL, not the other way.

So the "components write searchManager directly" pattern would mean: HeaderMenu calls `searchManager.selectFilterItem(key, item)` or similar, then EventListener watches `searchManager.selectedFilters` via $effect and enqueues FILTER.

However, this is a significant rewire of the filter flow. Currently the URL is the source of truth for filters (URL → searchManager sync). Changing to searchManager → URL would reverse the data flow.

**Decision: Keep the current filter flow.** The current pattern (component → uiCtx.handleFilterSelect → URL update → $effect → queue) works correctly. The "components write searchManager directly" aspiration from CONTEXT.md would require reversing the URL/searchManager data flow, which is a larger change better suited for a future URL redesign phase (explicitly deferred in CONTEXT.md's deferred section).

**Actual task — final cleanup and verification:**

1. In EventListener.svelte, verify the $effect that registers callbacks is clean:
```typescript
$effect(() => {
  uiCtx.handleFilterSelect = handleFilterSelect;
});
```
Make sure `uiCtx.applySort = applySort` is NOT in this $effect (removed in Plan 02).

2. In `gridContext.svelte.ts` UiContext type: Keep `applySort` and `handleFilterSelect` fields — they are the communication channels used by GridHeader and EventListener respectively. No type changes needed.

3. In `GridContextProvider.svelte`: Verify the uiCtx initialization has `handleFilterSelect: null` and `applySort: null` — these are correct initial values before the owning components register their implementations.

4. Do a final audit of EventListener.svelte to confirm it matches the target architecture from CLAUDE.md:
   - No `data` prop (individual props instead) ✓ (Plan 01)
   - No local `$state` for baseAssets/filteredAssets ✓ (Plan 01)
   - No `setNextIdProvider` $effect ✓ (Plan 01)
   - No sort functions ✓ (Plan 02)
   - Still has: event queue creation, $effects for reactive watching, URL sync, action callbacks on dataCtx, dirty cell tracking, realtime handler, filter panel/header menu mutual-close effects
   - This matches CLAUDE.md's EventListener spec: "creates event queue + handler, wires reactive effects that dispatch events, manages URL sync, registers callbacks on dataCtx"

5. Run `svelte-check` as the final gate.

6. If svelte-check passes, verify the complete phase success criteria from CONTEXT.md:
   - `+page.svelte` owns ALL server load data as `$state` ✓
   - EventListener receives only setter lambdas — no `data` prop ✓
   - New rows use "NEW-N" string IDs ✓ (already done in 06.1-03)
   - Sort logic lives in GridHeader ✓ (Plan 02)
   - `svelte-check` 0 errors ✓
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    EventListener is a pure event processor per CLAUDE.md target architecture. No sort code, no local data state, no dead code. UiContext callback fields are clean communication channels. Filter flow works through URL indirection (component → uiCtx → URL → $effect → queue). svelte-check 0 errors. Phase 7 complete.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json` — 0 errors
2. Final EventListener audit:
   - grep for `sortData` → 0 matches
   - grep for `$state(data.assets` → 0 matches
   - grep for `setNextIdProvider` → 0 matches
   - grep for `type Props = { data:` → 0 matches
3. Phase 7 CONTEXT.md success criteria checklist:
   - [x] +page.svelte owns ALL server load data as $state
   - [x] EventListener receives only setter lambdas — no data prop
   - [x] New rows use "NEW-N" string IDs (done in 06.1-03)
   - [x] Sort logic lives in GridHeader
   - [x] svelte-check 0 errors
</verification>

<success_criteria>
- EventListener matches CLAUDE.md target: event queue wiring, $effects, URL sync, action callbacks
- No sort functions in EventListener
- No local data $state in EventListener
- Filter selection works end-to-end (HeaderMenu → uiCtx → URL → $effect → queue)
- svelte-check passes with 0 errors
- All existing functionality works: sort, filter, view change, commit, discard, add row, edit, undo/redo
</success_criteria>

<output>
After completion, create `.planning/phases/07-row-generation-redesign/07-03-SUMMARY.md`
</output>
