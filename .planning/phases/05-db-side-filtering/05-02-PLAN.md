---
phase: 05-db-side-filtering
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/lib/components/grid/DataController.svelte
  - frontend/src/routes/api/search/+server.ts
  - frontend/src/routes/api/assets/view/+server.ts
  - frontend/src/lib/db/select/searchAssets.ts
  - frontend/src/lib/db/select/getAssetsByView.ts
autonomous: true
requirements: [F5.1, F5.2, F5.3, F5.4, F5.5]

must_haves:
  truths:
    - "DataController view-change fetch hits /api/assets?view=X (not /api/assets/view)"
    - "DataController search/filter fetch hits /api/assets?q=X&filter=k:v&view=Y (not /api/search)"
    - "Both fetch paths unwrap response as result.assets (consistent { assets, dbError } shape)"
    - "Clearing filters re-points to cached baseAssets without refetch"
    - "No client-side .filter() on assets array for search/filter operations"
    - "/api/search route no longer exists"
    - "/api/assets/view route no longer exists"
  artifacts:
    - path: "frontend/src/lib/components/grid/DataController.svelte"
      provides: "Updated fetch URLs pointing to unified /api/assets endpoint"
    - path: "frontend/src/routes/api/search/+server.ts"
      provides: "DELETED — consolidated into /api/assets"
    - path: "frontend/src/routes/api/assets/view/+server.ts"
      provides: "DELETED — absorbed into /api/assets"
    - path: "frontend/src/lib/db/select/searchAssets.ts"
      provides: "DELETED — renamed to queryAssets.ts in Plan 01"
    - path: "frontend/src/lib/db/select/getAssetsByView.ts"
      provides: "DELETED — replaced by queryAssets(null, {}, view)"
  key_links:
    - from: "frontend/src/lib/components/grid/DataController.svelte"
      to: "frontend/src/routes/api/assets/+server.ts"
      via: "fetch('/api/assets?...')"
      pattern: "fetch\\(['\"`]/api/assets"
---

<objective>
Migrate DataController's two fetch paths to the unified `/api/assets` endpoint and delete the three obsolete files: `/api/search/+server.ts`, `/api/assets/view/+server.ts`, `searchAssets.ts`, and `getAssetsByView.ts`.

Purpose: Complete the client-side migration so all asset data fetching goes through one endpoint with consistent response shape. Remove dead code to eliminate maintenance surface.

Output: Updated DataController with unified fetch paths; 4 deleted files; zero dead API routes.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-db-side-filtering/05-CONTEXT.md
@.planning/phases/05-db-side-filtering/05-RESEARCH.md
@.planning/phases/05-db-side-filtering/05-01-SUMMARY.md

<interfaces>
<!-- The unified endpoint created by Plan 01. DataController must match this contract. -->

From frontend/src/routes/api/assets/+server.ts (after Plan 01):
```typescript
// GET /api/assets?q=X&filter=location:ServerRoom&view=ped
// Response: { assets: Record<string, any>[], dbError: string | null }
// No params: returns all assets for default view
// With params: builds Kysely WHERE clauses via queryAssets()
```

From frontend/src/lib/components/grid/DataController.svelte (current fetch calls to update):
```typescript
// VIEW CHANGE PATH (line 223):
const response = await fetch(`/api/assets/view?view=${urlView}`);
// Response shape: { assets: [...] }
// Assignment: baseAssets = result.assets;

// SEARCH/FILTER PATH (line 260):
const response = await fetch(`/api/search?${params.toString()}`);
// Response shape: raw array []
// Assignment: filteredAssets = result || [];
```

CRITICAL: The search path currently expects a RAW ARRAY from /api/search.
After migration it will receive { assets, dbError } from /api/assets.
The assignment on line 275 MUST change from `result || []` to `result.assets || []`.

IMPORTANT: Do NOT delete getAssets.ts — mobile/manage/+page.server.ts imports getDefaultAssets from it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DataController fetch URLs and response unwrapping</name>
  <files>frontend/src/lib/components/grid/DataController.svelte</files>
  <action>
  Update the two fetch paths in DataController.svelte's URL-driven $effect:

  **View-change path (around line 223):**
  - Change: `fetch(\`/api/assets/view?view=${urlView}\`)`
  - To: `fetch(\`/api/assets?view=${urlView}\`)`
  - The response unwrapping (`result.assets`) is ALREADY correct for this path — no change needed.

  **Search/filter path (around line 260):**
  - Change: `fetch(\`/api/search?${params.toString()}\`)`
  - To: `fetch(\`/api/assets?${params.toString()}\`)`
  - CRITICAL response shape fix on line 275: Change `filteredAssets = result || []` to `filteredAssets = result.assets || []` — the old `/api/search` returned a raw array, but the unified `/api/assets` returns `{ assets, dbError }`.

  Do NOT restructure the $effect, the skipInitialFetch guard, the cancelled flag, or any other logic. Only change the two URL strings and the one response unwrapping line.

  Do NOT change any other fetch calls in DataController (commit, create, etc. use different endpoints).
  </action>
  <verify>
  Run from frontend/: `npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5` — 0 errors expected.
  Then grep to confirm no remaining references to old endpoints:
  `grep -n 'api/search\|api/assets/view' frontend/src/lib/components/grid/DataController.svelte` — should return nothing.
  </verify>
  <done>
  DataController fetches exclusively from `/api/assets` for both view changes and search/filter. Response unwrapping uses `result.assets` consistently for both paths. No references to `/api/search` or `/api/assets/view` remain in DataController.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete obsolete API routes and DB functions</name>
  <files>
    frontend/src/routes/api/search/+server.ts
    frontend/src/routes/api/assets/view/+server.ts
    frontend/src/lib/db/select/searchAssets.ts
    frontend/src/lib/db/select/getAssetsByView.ts
  </files>
  <action>
  Delete these 4 files (all callers have been migrated in Plan 01 + Task 1):

  1. `frontend/src/routes/api/search/+server.ts` — was called by DataController's search path, now pointing to `/api/assets`
  2. `frontend/src/routes/api/assets/view/+server.ts` — was called by DataController's view-change path, now pointing to `/api/assets`
  3. `frontend/src/lib/db/select/searchAssets.ts` — superseded by `queryAssets.ts` (created in Plan 01). No remaining imports.
  4. `frontend/src/lib/db/select/getAssetsByView.ts` — superseded by `queryAssets(null, {}, view)`. No remaining imports after Plan 01 updated `+page.server.ts`.

  Also delete the empty `frontend/src/routes/api/search/` directory and `frontend/src/routes/api/assets/view/` directory after removing their files.

  Do NOT delete `frontend/src/lib/db/select/getAssets.ts` — it exports `getDefaultAssets` used by `mobile/manage/+page.server.ts`.

  Before deleting, run a quick grep to confirm zero remaining imports of the functions being removed:
  - `grep -r "searchAssets" frontend/src/ --include="*.ts" --include="*.svelte"` — should only show `searchAssets.ts` itself
  - `grep -r "getAssetsByView" frontend/src/ --include="*.ts" --include="*.svelte"` — should only show `getAssetsByView.ts` itself
  - `grep -r "api/search" frontend/src/ --include="*.ts" --include="*.svelte"` — should return nothing
  - `grep -r "api/assets/view" frontend/src/ --include="*.ts" --include="*.svelte"` — should return nothing

  If any grep finds unexpected callers, do NOT delete that file — report the finding.
  </action>
  <verify>
  Run from frontend/: `npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5` — 0 errors expected.
  Confirm deletions: `ls frontend/src/routes/api/search/ 2>&1` should show "No such file or directory".
  Confirm getAssets.ts preserved: `ls frontend/src/lib/db/select/getAssets.ts` should exist.
  </verify>
  <done>
  4 obsolete files deleted. No stale imports remain. `svelte-check` passes with 0 errors. `getAssets.ts` is preserved for mobile route. The codebase has exactly one query path for asset data: `queryAssets()` in `queryAssets.ts`, exposed via `/api/assets` endpoint.
  </done>
</task>

</tasks>

<verification>
1. `npx svelte-check --tsconfig ./tsconfig.json` from `frontend/` — 0 errors
2. `grep -r "api/search" frontend/src/ --include="*.ts" --include="*.svelte"` — no results
3. `grep -r "api/assets/view" frontend/src/ --include="*.ts" --include="*.svelte"` — no results
4. `grep -r "searchAssets" frontend/src/ --include="*.ts" --include="*.svelte"` — no results
5. `grep -r "getAssetsByView" frontend/src/ --include="*.ts" --include="*.svelte"` — no results
6. `ls frontend/src/lib/db/select/getAssets.ts` — exists (mobile route preserved)
7. `ls frontend/src/lib/db/select/queryAssets.ts` — exists (unified function)
8. DataController has exactly 2 fetch calls to `/api/assets` (view-change + search/filter)
</verification>

<success_criteria>
- All asset data fetching goes through one endpoint: GET /api/assets
- DataController uses /api/assets for both view changes and search/filter
- Response shape is { assets, dbError } consistently — no raw array responses
- Clearing filters re-points to cached baseAssets (no refetch) — existing logic preserved
- 4 dead files deleted, 0 remaining stale imports
- getAssets.ts preserved for mobile/manage route
- svelte-check passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-db-side-filtering/05-02-SUMMARY.md`
</output>
