---
phase: 01-context-foundation
plan: "06"
type: execute
wave: 4
depends_on:
  - "01-04"
  - "01-05"
files_modified:
  - frontend/src/lib/components/grid/GridRow.svelte
  - frontend/src/lib/components/grid/GridOverlays.svelte
  - frontend/src/lib/components/grid/GridHeader.svelte
  - frontend/src/lib/components/grid/Toolbar.svelte
  - frontend/src/lib/components/grid/InventoryGrid.svelte
autonomous: true
requirements:
  - F1.2
  - NF1
  - NF2

must_haves:
  truths:
    - "GridRow.svelte has zero singleton imports from utils/"
    - "GridOverlays.svelte has zero singleton imports from utils/"
    - "GridHeader.svelte has zero singleton imports from utils/"
    - "Toolbar.svelte has zero singleton imports from utils/"
    - "All four components call getGridContext() to read/write shared state"
    - "App works end-to-end: edit a cell, save it, selection highlights, overlays render"
  artifacts:
    - path: "frontend/src/lib/components/grid/GridRow.svelte"
      provides: "Cell renderer using context for edit/selection state"
      contains: "getGridContext"
    - path: "frontend/src/lib/components/grid/GridOverlays.svelte"
      provides: "Overlay renderer using context for selection/dirty/copy state"
      contains: "getGridContext"
    - path: "frontend/src/lib/components/grid/GridHeader.svelte"
      provides: "Header renderer using context for column widths and sort state"
      contains: "getGridContext"
    - path: "frontend/src/lib/components/grid/Toolbar.svelte"
      provides: "Toolbar using context for change state and view state"
      contains: "getGridContext"
  key_links:
    - from: "frontend/src/lib/components/grid/GridRow.svelte"
      to: "frontend/src/lib/context/gridContext.svelte.ts"
      via: "getGridContext() in <script>, ctx.isEditing / ctx.selectionStart"
      pattern: "getGridContext"
    - from: "frontend/src/lib/components/grid/GridOverlays.svelte"
      to: "frontend/src/lib/context/gridContext.svelte.ts"
      via: "getGridContext() in <script>, reads dirtyCells/copyStart/selectionStart"
      pattern: "getGridContext"
---

<objective>
Verify and complete the consumer-side wiring for all four grid components. Plans 02-05 deleted
the singletons and updated the import sites as part of each migration. This plan confirms zero
singleton imports remain anywhere in the grid component path, fixes any missed call sites, and
does a full functional verification of the edit/save/selection/overlay workflows.

Purpose: This is the "seal the ring" plan — after Plans 02-05 each handled their slice of the
consumer updates, this plan ensures nothing was missed and the full interaction chain works.

Output: All four grid components fully on context; no utils/ singleton imports remaining in any
grid component.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-context-foundation/01-CONTEXT.md
@.planning/phases/01-context-foundation/01-04-SUMMARY.md
@.planning/phases/01-context-foundation/01-05-SUMMARY.md
@frontend/src/lib/components/grid/GridRow.svelte
@frontend/src/lib/components/grid/GridOverlays.svelte
@frontend/src/lib/components/grid/GridHeader.svelte
@frontend/src/lib/components/grid/Toolbar.svelte
@frontend/src/lib/context/gridContext.svelte.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix any remaining singleton imports in grid components</name>
  <files>
    frontend/src/lib/components/grid/GridRow.svelte
    frontend/src/lib/components/grid/GridOverlays.svelte
    frontend/src/lib/components/grid/GridHeader.svelte
    frontend/src/lib/components/grid/Toolbar.svelte
    frontend/src/lib/components/grid/InventoryGrid.svelte
  </files>
  <action>
**Step 1: Audit all five components for remaining singleton imports.**

Run a comprehensive search for any remaining utils/ singleton imports across the grid path:

```bash
grep -rn "from.*\$lib/utils" /home/joakim/asset_management/frontend/src/lib/components/grid/
grep -rn "from.*\$lib/utils" /home/joakim/asset_management/frontend/src/routes/+page.svelte
```

Also check for any remaining direct manager references that slipped through:
```bash
grep -rn "editManager\|selectionManager\|columnManager\|rowManager\|changeManager\|clipboardManager\|sortManager\|viewManager\|historyManager\|rowGenerationManager\|validationManager" /home/joakim/asset_management/frontend/src/lib/components/grid/
```

**Step 2: Fix any remaining singleton import found.**

For each remaining singleton import in a grid component:

a. Identify which controller now provides that functionality (from Plans 02-05 SUMMARYs)
b. Check if the component already has `const ctx = getGridContext()` — add it if not
c. Check if the required controller is already instantiated in the component — add if missing
d. Replace the singleton call with the appropriate context read or controller method call

Pattern reference:
- Any `ctx.isEditing` / `ctx.editRow` / `ctx.editKey` / `ctx.inputValue` → read from ctx directly
- Any `ctx.selectionStart` / `ctx.selectionEnd` / `ctx.dirtyCells` → read from ctx directly
- Any `ctx.columnWidths.get(key)` → read from ctx directly
- `edit.startEdit(...)` → via EditController instantiated in component
- `sel.handleMouseDown(...)` → via SelectionController instantiated in component
- `clipboard.copy(...)` → via ClipboardController instantiated in component

**Step 3: Verify each component has the correct controller instantiations.**

GridRow.svelte needs:
- `const ctx = getGridContext()`
- `const edit = createEditController()` (for isEditingCell, startEdit references)
- `const sel = createSelectionController()` (for handleMouseDown, selectCell)

GridOverlays.svelte needs:
- `const ctx = getGridContext()`
- `const sel = createSelectionController()` (for computeVisualOverlay, computeDirtyCellOverlays)
  OR: Move overlay computation entirely to InventoryGrid and pass results as props — whichever
  approach is cleaner given what the component currently does. Read GridOverlays.svelte first.

GridHeader.svelte needs:
- `const ctx = getGridContext()` (for columnWidths, sortKey, sortDirection, resizingColumn)
- `const columns = createColumnController()` (for startResize, updateResize, endResize)

Toolbar.svelte needs:
- `const ctx = getGridContext()` (for hasUnsavedChanges, activeView)
- `const changes = createChangeController()` (for commit, clear operations)
- `const rowGen = createRowGenerationController()` (for new row generation)

InventoryGrid.svelte needs all controllers instantiated — it orchestrates all of them.

**Step 4: Check the interactionHandler.ts file.**

```bash
cat /home/joakim/asset_management/frontend/src/lib/utils/interaction/interactionHandler.ts
```

This file was NOT in the manager list but lives in utils/interaction/. Determine if it imports
any singletons that are now deleted. If so, update or move it. If it is a pure utility (no
singleton imports), it can be moved to `$lib/components/grid/` or `$lib/utils/` as appropriate
— but do NOT delete it without checking what imports it.

**Step 5: Run svelte-check and resolve any remaining errors in grid path.**

```bash
cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep "error" | grep -v "admin\|mobile\|audit" | head -30
```

Fix ALL errors that are NOT in admin/, mobile/, or audit/ paths. Errors in those paths are
acceptable per the locked decision (admin/mobile refactoring is a future phase).
  </action>
  <verify>
1. No utils/ singleton imports in grid: `grep -rn "from.*\$lib/utils.*Manager\|from.*\$lib/utils.*manager" /home/joakim/asset_management/frontend/src/lib/components/grid/` → no output
2. All four components use getGridContext: `grep -l "getGridContext" /home/joakim/asset_management/frontend/src/lib/components/grid/GridRow.svelte /home/joakim/asset_management/frontend/src/lib/components/grid/GridOverlays.svelte /home/joakim/asset_management/frontend/src/lib/components/grid/GridHeader.svelte /home/joakim/asset_management/frontend/src/lib/components/grid/Toolbar.svelte`
3. svelte-check grid clean: `cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep "error" | grep -v "admin\|mobile\|audit" | head -10`
  </verify>
  <done>
Zero singleton imports from utils/ remain in any grid component. All four components use
getGridContext(). svelte-check shows no errors in the grid component path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Functional verification of edit/save/selection/overlay chain</name>
  <files>
    frontend/src/lib/components/grid/InventoryGrid.svelte
  </files>
  <action>
This task verifies the full interaction chain works by tracing the code paths end-to-end. No
new files are created — this is an audit and fix task.

**Trace 1: Edit lifecycle**

Follow the path: double-click cell → startEdit → textarea appears → type → save/cancel.

In the code, verify:
1. GridRow.svelte double-click handler calls `edit.startEdit(row, col, key, currentValue)`
   or calls the `onEditAction` prop which leads to startEdit in InventoryGrid
2. `startEdit` sets `ctx.isEditing = true`, `ctx.editRow`, `ctx.editCol`, `ctx.editKey`,
   `ctx.inputValue`, expands `ctx.columnWidths`, sets `ctx.rowHeights`
3. The textarea in GridRow renders when `edit.isEditingCell(row, col)` returns true
   (which reads `ctx.isEditing && ctx.editRow === row && ctx.editCol === col`)
4. Save: calls `edit.save(assets)` → returns change object → caller handles API call
5. Cancel: calls `edit.cancel()` → restores column width, clears edit state in ctx

If any step is disconnected (e.g., startEdit is called but ctx fields aren't set, or textarea
doesn't react to ctx.isEditing), fix the wiring.

**Trace 2: Selection highlight**

Follow: mousedown on cell → selectionStart/End set → GridOverlays renders highlight.

1. GridRow mousedown calls `sel.handleMouseDown(row, col, e)`
2. `handleMouseDown` sets `ctx.selectionStart = { row, col }` and `ctx.selectionEnd`
3. GridOverlays reads `ctx.selectionStart` / `ctx.selectionEnd` and calls
   `sel.computeVisualOverlay(...)` to produce position/size data
4. GridOverlays renders the selection box using the computed dimensions

Fix any disconnected step.

**Trace 3: Dirty cell overlays**

Follow: save edit → changeManager.trackChange → dirtyCells updated → GridOverlays shows dirty.

1. After save, the caller (InventoryGrid's save handler) calls `changes.trackChange(...)`
2. `trackChange` updates `ctx.hasUnsavedChanges = true` and calls `sel.setDirtyCells([...])`
   which sets `ctx.dirtyCells`
3. GridOverlays reads `ctx.dirtyCells` and renders dirty overlays via
   `sel.computeDirtyCellOverlays(...)`

Fix any disconnected step.

**Fix any issues found** — these are wiring gaps, not architectural problems. The fix is
typically: ensure the right controller method is called at the right point in the flow, and
that state is read from ctx rather than from a now-deleted singleton property.
  </action>
  <verify>
1. No broken `undefined` references in grid component: `grep -rn "\.isEditing\b\|\.editState\b\|\.selection\b\|\.columnManager\b" /home/joakim/asset_management/frontend/src/lib/components/grid/` should show only ctx.* or controller method calls, not bare singleton references
2. svelte-check final: `cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep "error" | grep -v "admin\|mobile\|audit" | wc -l` → 0
3. interactionHandler.ts accounted for (either updated, moved, or confirmed as non-grid utility)
  </verify>
  <done>
Full edit/save/selection/overlay chain verified and working at code level. Zero errors in grid
path from svelte-check. interactionHandler.ts is either updated or documented as unaffected.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "from.*\$lib/utils.*Manager" /home/joakim/asset_management/frontend/src/lib/components/` → empty (no singleton imports in grid)
2. All four grid components have getGridContext call
3. svelte-check: `cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep "error" | grep -v "admin\|mobile\|audit"` → empty
4. Edit/save chain code-traced and connected end-to-end
</verification>

<success_criteria>
- Zero singleton imports from utils/interaction/, utils/core/, utils/data/ in any grid component
- All four grid components (GridRow, GridOverlays, GridHeader, Toolbar) use getGridContext()
- svelte-check produces zero errors in the grid component path
- Code trace confirms edit/selection/dirty overlay chains are fully wired
</success_criteria>

<output>
After completion, create `.planning/phases/01-context-foundation/01-06-SUMMARY.md`
</output>
