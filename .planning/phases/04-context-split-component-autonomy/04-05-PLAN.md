---
phase: 04-context-split-component-autonomy
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - frontend/src/routes/+page.svelte
  - frontend/src/lib/context/gridContext.svelte.ts
autonomous: true
requirements: [F1.5, F2.1]

must_haves:
  truths:
    - "+page.svelte is under 60 lines"
    - "+page.svelte only calls set*Context($state({...})) and renders children — no business logic, no controllers, no effects"
    - "The old monolithic GridContext type and [getGridContext, setGridContext] pair are removed"
    - "All grid functionality still works: editing, selection, context menu, toolbar actions, virtual scroll"
  artifacts:
    - path: "frontend/src/routes/+page.svelte"
      provides: "Thin route wrapper"
      min_lines: 30
      contains: "setEditingContext"
    - path: "frontend/src/lib/context/gridContext.svelte.ts"
      provides: "Clean domain contexts only — no monolithic type"
      contains: "createContext"
  key_links:
    - from: "+page.svelte"
      to: "gridContext.svelte.ts"
      via: "set*Context calls for all ~10 domains"
      pattern: "set(Editing|Selection|Clipboard|Column|Row|Sort|Validation|Change|Data|View|Ui)Context"
    - from: "+page.svelte"
      to: "DataController.svelte + Toolbar + GridContainer + ContextMenu"
      via: "renders as children"
      pattern: "<(DataController|Toolbar|GridContainer|ContextMenu)"
---

<objective>
Rewrite +page.svelte as a thin wrapper (< 60 lines) and remove the deprecated monolithic GridContext.

Purpose: This is the capstone task. With all business logic in DataController, all components reading domain contexts directly, and all controller factories using domain getters, +page.svelte can be reduced to its true role: initialize contexts with default values and render children. The monolithic GridContext can be deleted.

Output: +page.svelte < 60 lines; monolithic GridContext type and [getGridContext, setGridContext] removed from codebase.
</objective>

<execution_context>
@/home/joakim/.claude/get-shit-done/workflows/execute-plan.md
@/home/joakim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-context-split-component-autonomy/04-RESEARCH.md
@.planning/phases/04-context-split-component-autonomy/04-04-SUMMARY.md

@frontend/src/lib/context/gridContext.svelte.ts
@frontend/src/routes/+page.svelte

<interfaces>
<!-- All set*Context functions available from Plan 01 -->
setEditingContext, setSelectionContext, setClipboardContext, setColumnContext,
setRowContext, setSortContext, setValidationContext, setChangeContext,
setDataContext, setViewContext, setUiContext

<!-- Children to render -->
DataController (renderless, receives {data} prop)
Toolbar (zero props)
GridContainer (assets prop or reads dataContext)
ContextMenu (zero props)

<!-- Target +page.svelte pattern from CONTEXT.md -->
<script lang="ts">
  import type { PageProps } from './$types';
  import { setEditingContext, setSelectionContext, ... } from '$lib/context/gridContext.svelte.ts';
  import { SvelteMap } from 'svelte/reactivity';
  import DataController from '$lib/components/grid/DataController.svelte';
  import Toolbar from '$lib/components/grid/Toolbar.svelte';
  import GridContainer from '$lib/components/grid/GridContainer.svelte';
  import ContextMenu from '$lib/grid/components/context-menu/contextMenu.svelte';

  let { data }: PageProps = $props();

  // Set all contexts synchronously
  setEditingContext($state({ isEditing: false, editKey: null, editRow: -1, editCol: -1, ... }));
  setSelectionContext($state({ selectionStart: {row:-1,col:-1}, selectionEnd: {row:-1,col:-1}, ... }));
  // ... ~10 set*Context calls with initial values
</script>

<div class="px-4 py-2 flex-grow flex flex-col">
  <DataController {data} />
  <Toolbar />
  <GridContainer />
  <ContextMenu />
</div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite +page.svelte as thin wrapper (< 60 lines)</name>
  <files>frontend/src/routes/+page.svelte</files>
  <action>
**Complete rewrite** of +page.svelte. The current file is ~1,199 lines. After Plans 01-04, most logic has been extracted. Now do a clean rewrite.

The new +page.svelte must:

1. **Import section (~10 lines):**
   - Import `PageProps` type
   - Import all `set*Context` functions from `$lib/context/gridContext.svelte.ts`
   - Import `SvelteMap` from `svelte/reactivity`
   - Import child components: `DataController`, `Toolbar`, `GridContainer`, `ContextMenu`

2. **Props (~1 line):**
   - `let { data }: PageProps = $props();`

3. **Context initialization (~25-35 lines):**
   - Call each `set*Context($state({...}))` with sensible initial values:
   - `setEditingContext($state({ isEditing: false, editKey: null, editRow: -1, editCol: -1, editOriginalValue: '', editOriginalColumnWidth: 0, inputValue: '', editDropdown: null, autocomplete: null }))`
   - `setSelectionContext($state({ selectionStart: { row: -1, col: -1 }, selectionEnd: { row: -1, col: -1 }, isSelecting: false, isHiddenAfterCopy: false, dirtyCells: new Set() }))`
   - `setClipboardContext($state({ copyStart: { row: -1, col: -1 }, copyEnd: { row: -1, col: -1 }, isCopyVisible: false }))`
   - `setColumnContext($state({ keys: [], columnWidths: new SvelteMap(), resizingColumn: null }))`
   - `setRowContext($state({ rowHeights: new SvelteMap() }))`
   - `setSortContext($state({ sortKey: null, sortDirection: null }))`
   - `setValidationContext($state({ validationConstraints: {} }))`
   - `setChangeContext($state({ hasUnsavedChanges: false, hasInvalidChanges: false }))`
   - `setDataContext($state({ assets: [], baseAssets: [], filteredAssetsCount: 0, user: data.user ?? null }))`
   - `setViewContext($state({ activeView: 'default', virtualScroll: null, scrollToRow: null }))`
   - `setUiContext($state({ filterPanel: null, headerMenu: null, contextMenu: null }))`
   - Note: DataController will enrich these contexts with actual data on mount

4. **Template (~5-8 lines):**
   ```svelte
   <div class="px-4 py-2 flex-grow flex flex-col">
     <DataController {data} />
     <Toolbar />
     <GridContainer />
     <ContextMenu />
   </div>
   ```

**CRITICAL rules:**
- NO $effect blocks
- NO controller creation (createSelectionController, etc.)
- NO business logic (commit, discard, filter, sort)
- NO callback functions
- ALL set*Context calls must be synchronous (no await, no $effect wrapper)
- The ONLY prop passed to any child is `{data}` to DataController

**Total target: well under 60 lines.** The research pattern shows ~50 lines achievable.

Delete ALL remaining old code from the file — do not leave commented-out sections.
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5 && echo "---" && wc -l src/routes/+page.svelte</automated>
  </verify>
  <done>+page.svelte is under 60 lines. Contains only imports, $props(), set*Context() calls, and child component rendering. No business logic. svelte-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove deprecated monolithic GridContext from gridContext.svelte.ts</name>
  <files>frontend/src/lib/context/gridContext.svelte.ts</files>
  <action>
1. **Search for any remaining getGridContext/setGridContext usage:**
   ```
   grep -rn "getGridContext\|setGridContext" frontend/src/
   ```
   If ANY file still imports `getGridContext` or `setGridContext`, update it to use domain-specific getters first.

2. **Once no consumers remain:** Remove from `gridContext.svelte.ts`:
   - The `GridContext` type definition (the large monolithic type)
   - The `export const [getGridContext, setGridContext] = createContext<GridContext>()` line
   - The `// DEPRECATED` comment block
   - Any `ActionsContext` type and its `createContext` pair (should already be gone from Plan 04)

3. **Clean up the file:** Ensure only domain types, their `createContext` pairs, and shared types (`GridCell`, `ValidationConstraints`) remain. The file should be clean, well-organized, with each domain type and its `createContext` pair grouped together.

4. **Verify no import breaks:**
   Run `svelte-check` to confirm nothing still references the removed exports.

The final `gridContext.svelte.ts` should contain:
- Shared types: `GridCell`, `ValidationConstraints`
- ~10-12 domain type definitions
- ~10-12 `createContext<T>()` pairs
- Required imports (createContext, SvelteMap, type imports)
- NOTHING else — no logic, no defaults, no factories
  </action>
  <verify>
    <automated>cd /home/joakim/asset_management/frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5 && echo "---" && grep -rn "getGridContext\|setGridContext\|GridContext[^a-z]" src/ | grep -v node_modules | grep -v ".planning" || echo "No monolithic GridContext references found"</automated>
  </verify>
  <done>Monolithic GridContext type removed. getGridContext/setGridContext removed. No file references them. gridContext.svelte.ts contains only domain types and createContext pairs. svelte-check 0 errors.</done>
</task>

</tasks>

<verification>
- `wc -l frontend/src/routes/+page.svelte` shows under 60 lines
- `grep -rn "getGridContext\|setGridContext" frontend/src/` returns zero matches
- `grep -rn "pageActions" frontend/src/` returns zero matches
- `grep -c "createContext" frontend/src/lib/context/gridContext.svelte.ts` shows ~10-12 calls
- +page.svelte has zero $effect blocks, zero function definitions (except $props destructuring)
- `svelte-check` passes with 0 errors
</verification>

<success_criteria>
- +page.svelte < 60 lines — thin wrapper only
- Monolithic GridContext completely removed
- All grid functionality still works via domain contexts
- svelte-check 0 errors
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-context-split-component-autonomy/04-05-SUMMARY.md`
</output>
