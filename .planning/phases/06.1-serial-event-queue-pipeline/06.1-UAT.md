---
status: diagnosed
phase: 06.1-serial-event-queue-pipeline
source: 06.1-01-SUMMARY.md, 06.1-02-SUMMARY.md
started: 2026-02-27T12:00:00Z
updated: 2026-02-27T13:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Debug queue logging visible in devtools
expected: Open browser devtools Console tab. Perform any action that touches the network. You should see `[EventQueue] QUEUED/START/DONE` with pending array and ~2s delay between START and DONE.
result: pass

### 2. Commit changes works end-to-end
expected: Edit a cell value, click Commit. The change saves to the database. Toast confirmation appears. The cell retains the new value after page reload.
result: pass

### 3. Discard changes works end-to-end
expected: Edit a cell value, click Discard. The cell reverts to its original value. Toast confirmation appears. No network error in console.
result: pass

### 4. Add rows works end-to-end
expected: Click Add Row to create a local scaffold row. Fill in required fields. Click Commit. The new row persists after page reload.
result: issue
reported: "New row is not getting committed. Success toast is popping up but is a false positive. Also no validation on new row columns for invalid values."
severity: major

### 5. Search/filter updates results
expected: Type a search term in the search bar or apply a column filter. The grid updates to show only matching rows. Console shows FILTER event flow.
result: pass

### 6. View switching loads correct data
expected: Switch to a different view. The grid reloads with the new view's data. Console shows VIEW_CHANGE event flow.
result: issue
reported: "Cannot queue another view while the current one is in the event queue. Switching default -> ped -> audit -> default before ped finishes loading does nothing and doesn't queue anything. Should queue all view changes in order."
severity: major

### 7. Rapid operations serialize correctly
expected: Quickly perform two operations back-to-back. The second event's START appears only AFTER the first event's DONE. No errors, no interleaving.
result: pass

## Summary

total: 7
passed: 5
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "New rows commit successfully and persist after page reload"
  status: failed
  reason: "User reported: New row is not getting committed. Success toast is popping up but is a false positive. Also no validation on new row columns for invalid values."
  severity: major
  test: 4
  root_cause: "Toolbar creates independent rowGen instance (always empty), so new-row Commit button never shows. Editing scaffold row cells routes through ChangeController (wrong tracker) which sends fake temporary IDs to /api/update — UPDATE matches 0 rows but doesn't throw, returning false success."
  artifacts:
    - path: "frontend/src/lib/components/grid/Toolbar.svelte"
      issue: "Line 7: createRowGenerationController() creates independent instance, hasNewRows always false"
    - path: "frontend/src/lib/components/grid/GridOverlays.svelte"
      issue: "Line 31: same independent rowGen instance; line 348: records new-row edits in ChangeController"
    - path: "frontend/src/lib/grid/components/context-menu/contextMenu.svelte"
      issue: "Line 14: same independent rowGen instance"
    - path: "frontend/src/lib/grid/utils/rowGeneration.svelte.ts"
      issue: "isValidValue() skips required-field checks (validation gap)"
  missing:
    - "Share rowGen via context (like changes/history controllers)"
    - "Detect scaffold rows in FloatingEditor and route to rowGen.updateNewRowField() instead of changes.update()"
    - "Add REQUIRED_FIELDS validation to rowGeneration"
  debug_session: ".planning/debug/06.1-add-row-commit.md"

- truth: "View changes queue in FIFO order — rapid view switches all execute sequentially"
  status: failed
  reason: "User reported: Cannot queue another view while current one is processing. Rapid view switches (default -> ped -> audit -> default) are dropped instead of queued."
  severity: major
  test: 6
  root_cause: "Two bugs: (1) $effect coalescing — handleViewChange() mutates reactiveUrl, but $effect batches to next microtask so intermediate URL values are lost. (2) Stale guard — line 309 checks viewCtx.activeView which only updates when handler starts processing, so return-to-current-view clicks are silently dropped."
  artifacts:
    - path: "frontend/src/lib/grid/eventQueue/EventListener.svelte"
      issue: "Line 309: stale guard drops return-to-current-view; Lines 219-248: $effect coalesces rapid URL changes"
    - path: "frontend/src/lib/grid/eventQueue/EventHandler.svelte.ts"
      issue: "Line 227: viewCtx.activeView set inside handler (too late for guard)"
  missing:
    - "handleViewChange should call queue.enqueue() directly instead of going through URL -> $effect"
    - "Remove stale guard on line 309 — queue handles serialization"
    - "Update URL as side effect of handler completing, not as trigger"
  debug_session: ".planning/debug/06.1-view-switch-queue.md"
